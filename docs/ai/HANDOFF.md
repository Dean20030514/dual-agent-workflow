# HANDOFF.md

> 本文件是仓库现状的极简交接;历史任务全部在 `docs/ai/archive/<日期-任务>/`。
>
> **9P 计划审降 medium 档 + 便携版 v3.4(2026-08-30 稍晚,`cbd62f8`,Routine 模式)**:9P 推理档 `high` → `medium`,9A/9B 维持 `high`。依据 = 同日实测(5 个项目**最新已结束**会话:99 次 Codex 运行、17.1h、单轮中位 554s;其中 9P 28 次 3.8h、p50 514s,且三类里最不收敛——某项目 9P 跑满 6 轮仍「修订后可批准」,另一项目归档记录的逐轮 blocking 数 `6 / 5 / 3 / 4` 不单调收窄)。理由:9P 审规划文件而非代码 diff,对深度代码推理需求最低,实现级精度本就归 9A/9B;**9A/9B 不动**是因为它们审真实 diff,是实测中唯一挡下 `[Product Blocking]` 的环节,降档最先丢的正是「读代码 + 构造反例」类发现。顺带三项:① **推理档取值收敛为按审别的唯一定义处**(`reviewer-prompt.md` → 双审隔离协议 ③),README / CLAUDE.md / `/plan` / 便携版摘要四处复述全改指针;② 9P 节自带可执行命令(防照抄 ③ 的 `high` 模板),且 verdict 与 raw log **按轮次命名** `9P_r<n>`——9P 是多轮复审,固定文件名会覆盖上一轮产物;③ 便携版按 README 规则**整体重生** v3.3 → v3.4(`git mv` 保留历史,旧版退役,桌面副本哈希一致)。一轮 Routine 临时独立审查:**通过、零 blocking**,其唯一非阻塞建议(减少精确档位复述)已采纳并做得更进一步(CLAUDE.md 那处改为不含具体值的警告)。**未验证**:尚无 medium 档 9P 实跑——省多少、发现质量掉不掉全部未知;回滚判据写在降档来历旁:单轮时长(基线 p50 514s)/ 每轮 blocking 条数 / Author 表态采纳率(基线 100%),采纳率明显下降即回滚(回滚 = 档位改回 `high`)。部署照惯例走**精确同步**(非 mirror-replace,理由见 2026-08-12 状态节末条的 82 文件),受管面 99/99 零漂移。
> **检索面教训(既有教训的新变体)**:本轮全仓 grep 用了 `--include="*.md" --include="*.txt"` 过滤,漏掉 `codex/config.example.toml` 的档位默认值(由独立审查检出;结论无害——审查命令带 `--ignore-user-config` 且档位显式,不会把 9P 拉回 `high`)。2026-08-15 那条「检索面必须含 `codex/`」需再加一句:**不要按扩展名过滤**——配置类的值常不在 `.md`/`.txt` 里。
> **审查门槛落地 + 便携版 v3.3(2026-08-30,`76c3138`…`4861875`,Routine 模式)**:给 9P blocking 与 `[Verification Blocking]` 各加**成立门槛**——前者要求写得出「按此计划执行会失败」的具体后果,后者要求点名具体证据缺口 + 一个可证伪的检查(明写**不要求 Reviewer 给出实际运行结果**,否则与其禁止实跑的纪律自相矛盾),斩断无法证伪的散文挑刺。顺带修三处**既存**缺陷:母本把「绕过 validation/auth」列在 Verification 举例(与其自身分类优先级冲突;reviewer-prompt 与便携版早已改对,唯定义处漏改)、reviewer-prompt 把「删测试藏错」无条件记 Product(与影响判据分叉,便携版侧已是纯指针)、便携版把八字段守护装置扩大到一切「机制 X 拒绝 Y」声称(严于母本,会误阻塞有合格正反样本的 lint/config 守护)。便携版按 README 规则**整体重生** v3.2 → v3.3(`git mv` 保留历史,旧版退役,桌面副本同步)。
> **三轮 Routine 临时独立审查,blocking 走势 3 → 2 → 4**:第 3 轮四条**全部成立**,但**全部源自第 2 轮的修补**——为门槛绑定「已冻结验收范围」的那条约束,拖出分模式范围权威、枚举式豁免清单、`dispute` 通道、四处 prompt 摘要四样配套,每样又长出新缺陷。人类裁决**撤掉病根**而非逐条修补:该约束整条撤销,其余七项成果保留。**它是按决定缺席的,不是被漏掉的**——起草理由、配套代价与放弃的那部分,见 `4861875` commit message;守护类声称的范围约束本就由「等价类的封闭方式」承载,未受影响。**这是 2026-08-15 三病诊断在规则层的重演**:给规则打补丁去堵规则的洞,补丁自身会长出新洞;分辨信号 = 新一轮 blocking 是否全部来自上一轮修补。
> **9P 逐轮收敛 + diff 预算上调(2026-08-27 稍晚,已收敛并合入)**:人类两裁决——① 9P 由「默认一轮」改**与 9A/9B 同律逐轮复审至收敛**(Blocking None ⇔ 可批准双向绑定;`9P round` fail-closed;轮次上限 3,达限进 Awaiting Human Adjudication 四条互斥出路含逐次批准延长);② ★ 单轮任务 diff 预算 **2000 → 4000 行**。三轮 Routine 审查收敛(3→1→0);复述点全仓同步(含 codex/ 与 portable);部署 99/99 零漂移;审查 holding 已清理。全档:`archive/2026-08-27-9p-iterate-and-budget/HANDOFF.md`。
> **9P 计划审已落地并收敛(2026-08-27)**:Critical 正式路径新增计划批准前**默认必跑**的 9P 计划审(fresh-context 审查,审规划文件;唯一定义处 = `claude/workflow/reviewer-prompt.md` → 9P 节)。五轮 Routine 独立审查 blocking 轨迹 **2→2→3→1→0** 收敛;顺带把 9A/9B 正文 diff 升级为带 `:(exclude)` 排除的机械隔离(修掉既有的 re-review verdict 泄露隐患)。portable v3.2 已按母本整体再生(1204 行,blob `d569fc9e…`);受管面已精确同步部署,**终审计 99/99 零漂移**(含 live `settings.json` 的 clangd-lsp 反向漂移 live→仓库晋升)。工作过程的 4 个 commit 已按人类指示 squash 为单一 commit 合入 main;仓外审查 holding 已清理。**唯一遗留**:9P 相关行为级重放证据,待下一个真实 Critical 任务实测——场景清单(2026-08-27 参数修正审查后扩充):首次 9B / 首次 9A / 带上下文 9A re-review / 9P round 2 缺与带上一轮摘要(拒审与闭合核验)/ Blocking None ⇔ 可批准双向绑定 / round 3 达限四分支裁决(含批准延长)。全档:`archive/2026-08-27-9p-plan-review/HANDOFF.md`。
>
> **第二次下游反馈闭环(2026-08-15,`40dea7f`)——真实重放抓到六轮审查漏掉的东西**:下游把母本逐字重放进一个**真没有 PLAN、真没有 Reviewer** 的项目后,回流四条,**全部核实成立**:① 证据 vs 假设末条「否则计划停在 Pending」——PLAN 是 Critical 专有,Routine 无等价形式;② 验收条款末条「超出即 `[Verification Blocking]`」——那是 Reviewer verdict 分类,默认 Routine 无 Reviewer 签发;③ 母本 8 处**裸 `reviewer-prompt.md`**,与已修的 `docs/workflow-design-notes.md` 同类,部署到项目后指不到;④ Payback-on-Touch 里 `**any \`docs/ai/archive/**/HANDOFF.md\` debts still bind**` 是 **prettier-hostile** 的——glob 的 `**` 与粗体标记相撞,母本自己不跑 prettier 所以无感,**任何跑 prettier 的下游都会静默丢空格**(与 CRLF 同一类:受管面在下游工具链下不稳定)。全部已修:①② 按模式分流、③ 统一绝对路径、④ 把粗体挪到 glob 外。
> **这轮最该记的**:①② 两条**六轮独立审查全部漏掉,而且第 6 轮的 `Body Executability Scan` 全表还明确把这两节判为「通过」**。抓到它们的是**真实重放**——把规则搬到一个真的缺少那些载体的环境里逐条执行。**静态审查是读文本推断,重放是跑**;后者对"载体不存在"这类缺陷的检出力明显更高。下游另提一条存疑(守护装置的「Reviewer 不运行装置,只核产物」),经核为**非缺陷**——那是 Reviewer 在场时的**负向角色约束**,不要求实例化 Reviewer,第 6 轮 `Reviewer-As-Carrier Check` 已专门裁定过。
> **待下游决定(非母本事项)**:其项目仓无 `.gitattributes`、文件为 CRLF,母本为 LF。已建议归一化 + 加 `.gitattributes`(理由:逐字重放下行尾不一致使字节比对失效;一次性全文件 diff 现在付最划算;否则任何写 CRLF 的工具都会让它重来——母本这边正是 Python `write_text` 每次编辑都在转换换行,归一化一次下次又变回去)。**是其项目的决定,母本不代定。**
>
> **下游反馈闭环 + 模式边界全面分流(2026-08-15,六轮审查通过,`33f8b11`…`59be822`)**:起因 = 一个下游项目按新母本逐字重放后,报回三处它无权修的母本缺陷(受管面)。修完后**六轮独立审查**,blocking 轨迹 **4 → 3 → 2 → 2 → 1 → 0**,末轮 `Body Executability Scan` **16 节全部通过**、`Reviewer-As-Carrier / Reverse Check / Unintended Consequences / Blocking / Non-Blocking` 全为 None,判**整批可标为已独立审查、可交付**。
> **实质产出**:① 母本与便携版**每个规则节都带模式标注**(`仅 Critical` / `两模式恒适用` / `Reviewer 实际运行时适用` / `按模式取`;唯一豁免 `Project Overview`——项目事实模板非规则节);② 凡标为两模式恒适用的节,**正文都给出了 Routine 的可执行形式**——diff 预算(`git diff --shortstat HEAD` + 未跟踪行数)、守护装置(对话内六步 + 人类当场确认输入域)、Payback 延期出口(对话内请求 + 人类明确批准)、角色分配落点、截图产物化、验证三分类的复核者(Routine 由**人类**确认三项,**不得静默拉 Reviewer**);③ 三份恒适用枚举(母本 Mode Scope / 便携 4.1 / 便携序节)为同一 7 项集合。
> **本轮暴露的四类 Author 失误(如实登记,供后续避坑)**:① **未验证就下全称声称**——「本清单即完整枚举」被一轮证伪,后又两次同类(「机械核对 0/0/0」「每节标题都写明作用域」),根因是写规则时违反了正在写的那条「声称不得超出判定实际覆盖的范围」;② **验证工具自身过期**——跨文件标注检查器带一份写于改动前的 skip-list,恰好排除了刚改过的两节,"0 不一致"对它们天然失明;③ **对"是否验证过"作不实陈述**——曾声称 diff 规模"现算了",实际把计算与 prompt 放进同一后台命令、输出从未可见(21/9 vs 实际 16/10),对应违反声称完成纪律的 RUN 步;④ **改一处漏同类**——每轮都只修被点名位置,尤其反复漏掉便携版内嵌的母本副本(§4.1),后改用跨文件按节标题机械映射才根治。
> **行尾**:仓库一直存 LF(三方 OID 对照);工作树与部署面曾变 CRLF,根因是 Python `write_text` 在 Windows 转换换行,已改用 `newline=<LF>` 并全量归一化。**另更正**:此前"99/99 零漂移"用的 `git hash-object` 会按路径应用 clean 过滤、对行尾不敏感,现改用**字节级 `read_bytes()` 比对**。
>
> **母本回流(2026-08-15,母本侧已收敛,待人类扫 diff)**:起因 = 做便携版 v3.2 时发现**母本反而落后于便携版**,加上人类转来的下游反馈。改动 12 文件 / 162 行,覆盖 `claude/workflow/{AGENTS,QUALITY_GATES,index,reviewer-prompt,AB-model-diagnostic}.md` · `claude/commands/{debug,final-review}.md` · `claude/CLAUDE.md` · 仓根 `AGENTS.md` · `README.md` · **`codex/AGENTS.md`** · 便携版(部分)。**主要新增**:① Mode Scope 补「单轮任务 diff 预算」+ 明确「**Routine + 临时 Reviewer 是合法状态且不构成模式升级**」;② Reviewer-Lightweight Protocol **拆两层**(第一层资源纪律恒适用 / 第二层证据载体按模式取,给出两条可直接复制的纯文本整句);③ 守护有效性装置加 **★ 模式分流**(判据两模式恒适用,Routine 形式 = 在对话里跑完同一套六步协议,**省掉的只有产物形式不是判据**);④ 新增 **停止事件优先级**(唯一定义处,收敛 `/debug` 里"第二次失败就停"与"自动派 fresh-context 重做"的既有互斥);⑤ fresh-context 拆三分支 + 入口受控;⑥ `codex/AGENTS.md` 端到端模式分流(原 94 行 `Routine` 命中 **0**);⑦ 修 3 处指不到的坏路径 `docs/workflow-design-notes.md`。
> **三轮独立审查(4→? blocking)**:第 3 轮判母本主定义已收敛(B4 已修复,B1/B2/S1/S2 的母本部分已修复),剩余 blocking **全部指向便携版未同步**;末轮另修母本仅剩两处(codex 顶部"每个 fix 在双审窗口结束后"对 Routine 不可满足——Routine 无窗口;`QUALITY_GATES:49` 把 Routine 守护证据缩写成单次负向运行,已改为指向 ★ 模式分流不局部枚举)。**已达轮次上限,未开第 4 轮。**
> **便携版未收敛,人类裁决为「重新拆任务」**:第 3 轮点名 6 处未同步(两条整句 / Mode Scope 的 Routine 临时 Reviewer 例外 / 守护六步在 `:95,257,689-694,943` 的旧简式 / final-review 与停止优先级入口 `:70,903-907`)。**根因是 Author 的执行方式错了**:本轮一直在母本与便携版**并行逐条打补丁**,而便携版的既定纪律是「**按母本整体重新生成,不逐条补课**」(README 文首明令,也是当初做 v3.2 的全部理由)——于是每修一处母本就漏一处便携版,三轮累积成 6 处。便携版当前含**部分**同步(层 1 契约 / 守护 Routine 形式 / §11.1),**下一任务须按母本重新取受影响各节,不得信任这份部分状态**。
>
> **进行中(2026-08-15,待人类扫 diff + commit)**:`portable/` **v3.2 受控整体迁移**已产出——新增 `portable/通用prompt-v3.2.txt`(审后 1072 行,blob `c85a48bb…`),按 `main` 母本整体重新生成,非逐条补课。Routine 模式执行(文档任务)。diff 预算:新增 1072 行 + 三个已跟踪文件 12 行 ≈ 1084 / 2000,**在预算内,无需切片**。验证方式 = **带负向对照的机械覆盖检查**:26 个机制锚点在 v3.2 命中 ≥1 **且**在 v3.1 命中 0(负向对照证明锚点有区分力,不是"本来就在的词"),26/26 PASS;反向丢失检查 22/22 无丢失;另核对入库行尾(手工 LF 版与 clean 过滤后 OID 一致,与 v3.1 blob 同为 LF)、主节锚点 18 处无代码块污染、外部路径引用 4 类均为 0(便携自洽)。**同批已落**:`README.md` 便携行、`claude/CLAUDE.md` 便携版指针改指 v3.2(受管镜像面,本机 `~/.claude/CLAUDE.md` 需人类手动同步——installer 仍被迁移期 guard 锁定)、桌面副本 `Desktop\通用prompt-v3.2.txt` 已就位且与仓内哈希一致。
>
> **第 1 轮独立审查(Codex,保真度审查,非 9A/9B)**:判**不通过**,6 blocking + 2 suggestion,逐条回文件核实后 **6 条 blocking 全部成立**(Routine 丢失硬停与系统化调试 / 全局 ≤200 行与"完整测试输出"冲突且母本无此规则 / 条件触发的等价类枚举被写成无条件 / 9B 自检写成"同 9A"却自称自包含 / 4.1 标题声称"全文所有硬规则唯一定义处"与实际 21 处 ★ 分布矛盾 / sub-agent 降级例外条款母本没有)。已全部修复:5 条采纳、1 条(B5)修改后采纳——**不采纳其"删除核心原则 11 与第三节声称表中负向对照摘要"的部分**,技术理由:那两处是带指针的一行摘要、不含判据,与母本自身约定同构(`index.md` 原则 6、`implement.md` 声称表都是这么写的);真正的缺陷是标题声称,已改。2 条 suggestion 全部采纳。verdict 与 raw log 在仓外 `~/.codex-review-holding/portable-v32/`。
> **第 2 轮独立审查(re-review)**:判**不通过**。8 条修复中 **6 条确认已修复**(B2/B3/B4/B5/S1/S2,均给出母本对位证据);**B5 复议撤回原判断**——Reviewer 认可 Author 的技术理由(带指针的一行摘要不含判据,与母本 `index.md:21`、`plan.md:15` 的"摘要+权威指针"模式同构),不维持原 blocking。**B1、B6 判定为「修复引入新问题」,另报 3 条 blocking,全部是 B1 修复的连锁后果**:① 硬停被写成"有 Reviewer 时按 4.1 Fix-Loop"(按 Reviewer 在场触发),而母本 `AGENTS.md:11-12` 明确 SHA 绑定与双审协议**仅 Critical**、任何模式下 Reviewer 只带来零写入——**本次会话自身即反例**(Routine 跑了 Reviewer,却无 Fix-Loop Counter 与 Reviewer 判定的 `caused_by_last_fix`);② 641 行"必须派全新上下文 sub-agent" × 974 行"不能保证同模型就别派、由 Author 承担" × 582 行"协议扩到 Routine"三者互斥,且 Routine 无 TASK_BRIEF/批准计划可喂;③ 框定句只覆盖"第零～十五节",而「核心原则」在第零节**之前**,致原则 1~4 在 Routine 按字面无条件生效。**3 条均回文件核实成立,已修复**:硬停按模式(非按 Reviewer 在场)分流并写明"跑一次 Reviewer 不隐式升级 Critical";641 行拆成 Critical/Routine/无法保证同模型三分支;核心原则加模式适用表并把框定句扩到该节。审后 1085 行,blob `94a5b471…`。verdict 在仓外 `~/.codex-review-holding/portable-v32/review2.md`。
> **第 3 轮独立审查 → `stopped, NOT converged`(触发轮次上限,交人类裁决)**:判**不通过**。r2 的 3 条均判「部分修复」,另报 3 条 blocking——**仍然全部落在模式路由边界这一个面上**:① 44/45 行说"Reviewer 出现只带来零写入 + 4.1 轻量协议",但该协议(287-289 行)规定"结论只基于交接文件 + git diff + last_test_run.txt",而 Routine 明确没有前者与后者——**本次会话自身即证据:三轮 review 之所以能跑,是因为 Author 在 prompt 里额外覆盖了这条,不是文件自洽**;② 44 行"Routine 二次失败即停手报告人类" vs 648 行"失败 2 次后回退并立即派 fresh-context 重做"(无人类授权门),且 Critical 的 381 行硬停"禁止再试一轮"与 648 行的自动重启没有优先级排序;③ 原则 11 标"两模式恒适用",但其定义(4.1 守护有效性装置)必填 `tested_sha`、HANDOFF、PLAN 的 Frozen Acceptance 冻结输入域、Reviewer Verification Needed——Routine 一个都没有。**三条均回文件核实成立,是可执行矛盾而非叙述瑕疵。**
> **Reviewer 元判断(第 3 轮专问)**:模式路由面**尚未整体自洽,需要重构表述,不建议继续逐句打补丁**;并给出结构方案(模式路由放唯一规范矩阵 / 单列停止事件优先级 / 轻量协议拆成"恒适用资源纪律"与"仅 Critical 落账形式" / 核心原则改"恒适用实质 + Critical 实现形式"双栏 / 第八节只引用不复述阈值)。
> **按母本规则本轮不再自动续审**:轮次上限已到,且重构属设计变更、超出 ★ review-fix 最小生产范围(扩大架构须停下重新计划)。**当前状态记 `stopped, NOT converged`,v3.2 未过收敛门。** 三条 blocking 影响的是 Agent 在**默认 Routine 模式**下的实际行为,按分类语义属 Product 级,故按三者优先级② **不得当作"带限制交付"合入**。
> **人类裁决(2026-08-15)**:① 出路 = **重新拆任务重构**(不走"带限制交付",不回退);② 指针**保持指向 v3.2**——理由:三条缺陷都是"Routine 下某规则指向 Critical 产物",人类已知情;而 v3.1 缺 2026-08 全部机制,实际风险更大。
> 遗留 1 条 Cannot-Verify-From-Diff:目标环境 `@` 加载 1085 行文件是否会截断,未实测。三轮 verdict 与 raw log 全在仓外 `~/.codex-review-holding/portable-v32/`(prompt_r1~r3 + review1~3 + raw log,人类确认后可删)。
>
> **便携版模式路由表述重构(2026-08-15 已执行,待人类扫 diff + commit)**:Routine 模式。重构 diff = 59 增 / 35 删 = 94 行(远低于 2000 预算);便携版 1085 → 1123 行,blob `b43c01f6…`,桌面副本已同步。**Payback-on-Touch 生效**:本任务触碰 `portable/通用prompt-v3.2.txt`,三条 `[DEBT]` 同批还清。五步结构方案**全部落地**:① 序节新增 **★ 规范矩阵**(12 行 × 3 列:Routine / Routine+临时 Reviewer / Critical,逐项给交接文件·证据载体·Reviewer 依据·零写入·快照自检·SHA 闸门·diff 闸门·退出码闸门·硬停判据·Fix-Loop 账本·Agent 建 commit·人类批准形式),并明写"跑一次 Reviewer 不构成模式升级";② 新增 **★ 停止事件优先级**(Critical 正式硬停 > Routine 两次失败先报人类 > 仅在前两条未命中或人类明确要求重启才进 fresh-context;回退前须说明丢弃什么并等确认),**fresh-context 入口改为受控**、明写"不是绕过硬停的第四条出路";③ **Reviewer-Lightweight Protocol 拆两层**——第一层(资源纪律 + 零写入)任何模式恒适用,第二层(证据载体与落账)按模式取用,并明写 Routine 侧"没有交接文件与 last_test_run.txt,不得为此临时创建,也不得因其缺失而拒审、不执行快照自检与 SHA 绑定";④ **核心原则改三栏**(恒适用的实质 / Routine 的形式 / Critical 的形式),取消"某条原则在 Routine 不适用"的粗分,原则 11 补出 Routine 形式(对话内展示真实负向命令与退出码,做不到就当场收窄或撤回声称);⑤ 第八节改为**只引用**停止判据,不再复述阈值与权限。另采纳 r3 两条 Suggestion(原则 9 的模式标签、Critical 合法跳过 Reviewer 时 SHA 闸门记 N/A + 原因)。**验证**:三条债务偿还各带负向对照(新版 ≥1 / 重构前快照 = 0)、旧冲突措辞 4 类清零、26 机制覆盖回归 0 FAIL、22 项反向无丢失、主节锚点 18、外部路径引用 0、入库为 LF;并对 r3 提的四个组合(Routine 无/有 Reviewer、Critical 硬停、不保证同模型)逐一确认**各只有一处给判据**。**第 1 轮独立审查(本任务的审查预算)**:判**不通过**,4 blocking + 1 suggestion,但同时判「**结构方向已经正确,剩余问题可定点修补,不需要再次整体重构**」——重构路线本身被确认。四条回文件核实**全部成立**:① 轻量协议第一层(恒适用)里的守门句仍带 Critical 载体`以 last_test_run.txt 为准`,与第二层 Routine 分支互斥,第三节另有 3 处无模式限定的同类要求;② 守护有效性装置这个「唯一定义处」本身仍无条件要求常驻装置/`tested_sha`/PLAN 冻结输入域/Reviewer 落账,致核心原则表里写的 Routine 形式在执行时落回 Critical 产物;③ 第八节仍复述"第二次失败"阈值并给出与中央规则互斥的动作(回阶段 1 继续调查 vs 立即停手报告人类);④ Critical 硬停漏了母本 `AGENTS.md:171` 的「未获人类确认不得继续」,弱化人类裁决权。**已全部修复**:守门句拆成"恒适用资源纪律"+ 按模式追加的证据载体(Critical 侧仍逐字保留母本原句,已机械校验 9A/9B 各命中 1 次);守护装置节首加 ★ 模式分流(判据两模式恒适用,证据形式分 Critical/Routine);第八节删净次数与停止动作、改为只引用 ★ 停止事件优先级,该节 ② 同时扩为两模式恒适用;硬停恢复人类确认门并补"三条出路一律先停下交人类裁决"。Suggestion(PLAN 骨架多出母本没有的 `## Diff Budget Estimate` 独立标题)采纳,并回 `Architectural Layers & Split Assessment` 节内。**归因更正**:Reviewer 对 ④ 标 `caused_by_last_fix: yes`,但对重构前快照 grep 该句命中 0——是最初生成 v3.2 时即漏,非重构引入,故实际为 `no`;缺陷照修,归因按机械证据更正(本任务 Routine,无 streak 账本,不影响计数)。累计重构 diff 76 增 / 48 删,便携版 1128 行,blob `d7a4191b…`,桌面已同步。
> **第 2 轮独立审查(re-review)**:判**不通过**,但**只剩 1 条 blocking**——B2/B3/B4/S1 与"拉直守门句"全部判**已修复**,`Unintended Consequences: None`,四个组合中**三个已自洽**(Routine 无 Reviewer / Critical 有 Reviewer / 不保证同模型)。**归因复议:改判 `no`**,Reviewer 自行去翻两轮 raw log 找到旧版本的命令输出作机械证据,认同 Author 的更正(非采信自述)。剩余 1 条是 Author 上轮的疏漏:第 1 轮点名的第三节三处(223/225/252)只改了两处,**漏了第 5 条(225)**——该条无模式限定地要求 Reviewer 读 `last_test_run.txt`、并让 Author 把代跑结果追加进该文件,与 Routine 分支"不存在且不得创建"互斥;且第三节自称"全程强制",按本文件的按节读取协议会被单独读到,不能指望读者回看序节矩阵。**已修复,且按类扫全**:不止补第 5 条——给第三节加了**节级范围声明**(实质两模式恒适用、载体按模式取、以 ★ 规范矩阵为准),并把第 1、2、3、5 条**逐条分流**;第 5 条明确 Routine 侧"完整输出与退出码贴回对话,不得创建或追加 `last_test_run.txt`"。另全文扫过 `last_test_run` 的全部出现处,确认其余"无限定"处均落在本身即 Critical 专属的节内(第四节交接文件 / 第九节 9A/9B / 第十四节快速版 / 第十五节完成标准)。累计重构 diff 82 增 / 51 删,便携版 1132 行,blob `250e172a…`,桌面已同步。
> **第 3 轮独立审查:通过 —— 本任务已收敛**。`Blocking Issues: None` / `Non-Blocking Suggestions: None` / `Unintended Consequences: None`;**四个组合全部判"已自洽"**(Routine 无 Reviewer / Routine + 临时 Reviewer / Critical 有 Reviewer / 不保证同模型);`Convergence Judgement: 可标已收敛可交付`。三轮 blocking 轨迹 **4 → 1 → 0** 单调收窄。
> **一处 Author 更正(如实登记)**:Author 在第 3 轮声称"其余无模式限定的 `last_test_run` 引用只落在第四/九/十四/十五节",Reviewer 独立复扫指出**该枚举不完整**——第二、八、十、十一、十二节亦有(如 209/645/654/887/943/987 行)。**结论仍成立**(Reviewer 逐条给出各自的 Critical 限定来源:第 203 行限定第二节、第 635-636 行限定第八节、第 39 行覆盖第十~十二节完整仪式、943 行回指已分流的第三节、987 行允许手动验证产物),但**Author 的章节枚举当时是不完整的**,记此备查:凡"按类扫全"的声称,枚举本身也要经独立复核。
> **发现的母本缺口(新问题,未修,待人类定)**:母本 `claude/workflow/AGENTS.md:10-12` 只说"Reviewer 零写入任何模式恒适用"、并把"Reviewer 与双审协议"整体划归仅 Critical,**但从未定义「Routine 下临时运行的 Reviewer 依据什么下结论」**(已 grep 确认母本全文无此定义)。便携版 v3.2 现已定义(★ 规范矩阵 + 轻量协议第二层 Routine 分支 + 第三节第 5 条)。**这形成反向不一致:便携版比母本更完整。** 按快照仓 SSOT 与"受管面可复用修改须经仓库可见 diff 晋升"的原则,该定义应回流母本(`AGENTS.md` 的 Mode Scope 与 Reviewer-Lightweight Protocol、`reviewer-prompt.md`)。**不在本任务范围内**——属新任务,开工前照例过 diff 预算评估。
> 三轮 verdict 与 raw log 在仓外 `~/.codex-review-holding/portable-v32-restructure/`。
>
> **两 commit 独立审查(2026-08-15,两轮通过,`f5e20c6`)**:人类要求对 `564061d` + `17d5dd7` 补做独立审查——此前登记的"未经复审"实为**三处**(codex 顶部修复时序 / `QUALITY_GATES:49` / 便携版守护第 ⑤ 步,末者是审查通过后才采纳的 Suggestion,原报告漏登记)。审查对象 = 确切 commit range `fa7b874..17d5dd7`,工作树干净。**第 1 轮**:三处未审改动**逐条判正确**、`Master vs Portable: 一致`,但另抓到 **1 条 blocking**——`codex/AGENTS.md` 把 Critical Reviewer 的证据范围写成「`last_test_run.txt` + `git diff` **only**」,与母本 `AGENTS.md:88`「交接文件 + git diff + last_test_run」、同文件自身的 Critical 阅读清单、9A/9B 输入契约**三处冲突**;按 only 执行,Critical Reviewer 不能拿 TASK_BRIEF/PLAN 推理,等于废掉 9A 的主要职责。**来源**:该 `only` 在 `fa7b874` 即已存在(既有措辞),但拆两层时被带进 Critical 分支,**从模糊表述升格为绑定规则**——重构把既有瑕疵提升为硬规则,比新写错更隐蔽。已改为与母本对齐并写明盲审只省略 PLAN;同批采纳 Suggestion,删除 `AB-model-diagnostic.md:24` 的本地阈值复述「2 次失败就换 fresh sub-agent」。**第 2 轮通过**:两处修复确认、`Unintended Consequences: None`、`Restatement Sweep: None`、`Blocking/Non-Blocking 均 None`、**四种运行组合全部唯一可执行**(含上轮判"不唯一"的 Critical + Reviewer)、`可以标为已独立审查、可交付`。**至此 `fa7b874..f5e20c6` 全部改动均已独立审查,无遗留未审项。** 部署:受管面 2 文件已同步(备份 `.bak-20260815-173514`),复审计 **99/99 零漂移**,82 个仅本机归档完好。
>
> **便携版按新母本重新同步(2026-08-15 已完成,一轮审查通过)**:按母本**重取整节**而非逐条补那 6 处。共重取 **9 处**——审查点名 6 处 + Author 自查另发现 3 处,其中**第八节回归安全修复协议整句缺失**(母本 `debug.md:44` 有守护证据指针、便携版没有)只有对着母本重取才会暴露,逐条补发现不了。**独立审查一轮通过**:9/9 已同步、`Master vs Portable: 一致`、`Backport Gaps: None`、`Unintended Consequences: None`、四种运行组合全部自洽、`可以标为与母本对齐并交付`;唯一 Suggestion(守护六步第 ⑤ 步少「完整输出 + 退出码」)已采纳,现与母本 `AGENTS.md:147` 逐字一致。**验证**:Critical 守门句在母本 2 处 ↔ 便携版 3 处逐字一致;三类旧简式命中清零;26 机制 0 FAIL;22 项反向无丢失;主节锚点 18;外部路径 0;入库 LF。1133 → 1142 行,重同步 diff 19 增 / 12 删,blob `55268377…`,桌面副本已同步。**便携版与母本现已对齐,可作为同步源。**
>
> ~~下一件任务(人类已裁决,未开工)~~:**便携版按新母本重新同步**——已完成,见上。原方案:**性质是按母本重新生成受影响各节,不是逐条补那 6 处**——逐条补正是本轮被证明会持续漏项的做法。受影响面 = 母本本轮动过的全部规则(Mode Scope 的 Routine 临时 Reviewer 例外 / 轻量协议两层与两条可复制整句 / 守护 ★ 模式分流的六步 / 停止事件优先级 / fresh-context 三分支 / diff 预算入恒适用清单)。开工前过 diff 预算评估;完成后须复核便携版与母本**判据一致**(措辞可不同,结构本就不同)。
>
> ~~上一件任务~~:**便携版模式路由表述重构**——已于 2026-08-15 执行,见上。原方案记录:性质是**设计变更,不是接着打补丁**——三轮审查的 blocking 全部落在同一面,根因是:母本靠**文件分离**表达模式边界(`AGENTS.md` 有独立 Mode Scope 节声明哪些产物仅 Critical,每个 command 各自带模式标签),单文件版压平了这个结构,只能靠每条规则自带内联限定词,于是每加一条限定就要与其余全部规则重对一遍,持续漏。结构方案(Reviewer 给出 + Author 认可):① 模式路由放**一张唯一规范矩阵**(模式 × Reviewer 是否运行 × 证据载体 / SHA 闸门 / 硬停判据 / 是否需人类授权);② 单列**停止事件优先级**(Critical 正式 Fix-Loop/轮次上限 > Routine 两次失败先报告人类 > 仅在未触发正式硬停或人类明确要求重启时才进 fresh-context 分支);③ **Reviewer-Lightweight Protocol 拆两层**——两模式恒适用的资源纪律与零写入 / 仅 Critical 的 HANDOFF·last_test_run·Verification Needed 落账形式;④ 核心原则改**"恒适用的实质" + "Critical 的实现形式"双栏**,不再按整条编号粗分;⑤ 第八节只引用硬停与重启判据,不复述阈值或权限。开工前照例先过 diff 预算评估。
>
> **收尾待办(未执行,留到重构任务收尾)**:删除 `portable/通用prompt-v3.1.txt` 与桌面 `通用prompt-v3.1.txt`。人类原定「先让 GPT 审查,删除最后收尾再删」——三轮审查已跑完,但结论是 `stopped, NOT converged` 且重构任务未开工,**故 Author 未执行删除**:在未收敛状态下删掉唯一的树内回退副本不谨慎,而保留它的成本仅为两个无引用文件(指针均已指向 v3.2)。git history 留有 v3.1 全档,何时删都不丢。人类要现在删可随时指示。

## 状态(2026-08-27 更新)
* **9P 计划审落地**:重流程在批准门前新增默认必跑的独立计划审(判据唯一定义处 = `claude/workflow/reviewer-prompt.md` → 9P);9A/9B 正文 diff 改为带 `:(exclude)` 排除的机械隔离;portable v3.2 同步整体再生;受管面部署终审计 99/99 零漂移。全档与五轮审查轨迹:`archive/2026-08-27-9p-plan-review/HANDOFF.md`。
* **同日参数修正**:9P 改逐轮复审至收敛(双向绑定 / `9P round` fail-closed / 达限四条互斥出路含逐次延长);★ 单轮任务 diff 预算 2000 → 4000 行。三轮审查收敛(3→1→0)。全档:`archive/2026-08-27-9p-iterate-and-budget/HANDOFF.md`。

## 状态(2026-08-15 更新)
* **流程修正(2026-08-15)**:四个真实项目的 Critical 任务**全部** `stopped, NOT converged`/硬停,诊断出三病——① 证据层自指螺旋(后段 blocking 几乎全 `[Verification]`,多轮双审零 `[Product]`,docs/ai 证据面占 diff 41–74%);② 声称被**无区分力样本**验过(某 AC 的七个探针中五个本该被拒的全部通过,此前"验证通过"样本在机制不存在时也会照样通过);③ **判定权不在命令而在人读**(退出码类 AC 四轮零缺陷,"Reviewer 逐条核散文清单"类 AC 逐轮产新 blocking)。已落库**六条**修正:单轮 diff 预算 / streak 只由 Product 递增 / 轮次上限 / 证据层出口 / 负向对照扩到一切守护声称 / 验收必须可复现判定(`9dd07e1` + `018579b`)——**判据与数值一律以 `claude/workflow/AGENTS.md` 各自唯一定义处为准,本文件不复述**。经**两轮**外部审查(两轮均判不通过,共 14 条 blocking,逐条回代码核实后全部成立)修补至今:第 1 轮补分类优先级、出口文件范围、等价类覆盖、可复现判定、三者优先级、砍仪式护栏,并把 `/debug` `/final-review` `/plan` `/implement` `/define` 与 HANDOFF 模板的复述收敛为指针;第 2 轮补 **reviewer-prompt 的分类契约**(Reviewer 实际读的那份,此前仍把绕过 auth 列为 Verification)、**证据层出口与收敛门的衔接**(此前三条互斥使出口成死条文)、**`review_sensitive_paths` 清单本身冻结**、**等价类绑定人类冻结的输入域 + Reviewer 主张新类须给域内反例**、**反例须实际触发失败**、Safety Rules 与 README 的复述指针化。完整诊断与实测数据:auto-memory `critical-mode-diagnosis-2026-08-15`。
* **五轮外部审查已收口(2026-08-15)**:六条修正经 5 轮 Codex 审查逐轮修补,blocking **7→7→3→2→1** 单调收窄,第 5 轮 `Unintended Consequences: None`(修补不再产生新问题)。commit 链:`9dd07e1`(diff 预算)→`018579b`(五条)→`af8dce6`(r1 七条)→`8d1a0a3`(修法必附契约)→`3a0035a`(r2 七条)→`c6421d4`(r3 四条)→`b5cbb9d`(r4 一条)。**审查过程新增一条规则**:`reviewer-prompt.md` → **修法必附**——Reviewer 每条 Blocking/Suggestion 必附 `Proposed Fix`(具体改法 + 依赖假设;需取舍写"需人类裁决"),Author 逐条三选一表态(采纳/修改后采纳/不采纳;不采纳允许零改动但须技术理由;Suggestion 不影响通过)。**贯穿五轮的同一个病**:改「唯一定义处」时忘记同一规则在别处的复述——共发现 4 类复述点(`/debug` `/final-review` `templates/HANDOFF.md` `/plan` `/implement` `/define`、README、仓根 AGENTS.md、**`codex/AGENTS.md`(三次)**、以及 AGENTS.md 自身 `:183` 对 `:158` 的复述)。**教训:改规则前先全仓 grep 该规则的复述副本,检索面必须含 `codex/`。**
* **技术债明账(2026-08-15 登记 → 同日已还)**——三条均在「便携版模式路由表述重构」任务中偿还(Payback-on-Touch 生效:该任务正是触碰 `portable/通用prompt-v3.2.txt`,故同批还清)。偿还方式见文首重构记录;偿还证据 = 带负向对照的 grep 断言(新版命中 ≥1 且重构前快照命中 0)。**以下三条保留原文备查,状态: 已还**:
  * `[DEBT] 便携版 v3.2 的 Reviewer-Lightweight Protocol 未分层,Routine 临时跑 Reviewer 时该协议要求读不存在的交接文件与 last_test_run.txt | Payback trigger: portable/通用prompt-v3.2.txt,下次触碰该文件前必须先还 | Impact: Routine 下调用 Reviewer 需人类在 prompt 里额外覆盖规则才能执行,文件本身不自洽`
  * `[DEBT] 便携版 v3.2 的"二次失败停手报告人类"(44/589 行)与"回退并立即派 fresh-context 重做"(648 行)无优先级,Critical 侧正式硬停与自动重启同样未排序 | Payback trigger: portable/通用prompt-v3.2.txt,下次触碰该文件前必须先还 | Impact: 同一触发事件得到两条互斥指令,fresh-context 可能被当成绕过硬停的第四条出路`
  * `[DEBT] 便携版 v3.2 的核心原则 11 标"两模式恒适用",但其定义依赖 tested_sha/HANDOFF/PLAN 冻结输入域等 Critical 专有产物,Routine 无等价执行形式 | Payback trigger: portable/通用prompt-v3.2.txt,下次触碰该文件前必须先还 | Impact: Routine 下该原则无可执行形式,或反向逼出 Critical 产物、违反模式路由`
* **残余项(如实登记,人类可接受)**:① 便携版滞后**已由 v3.2 迁移解决**(2026-08-15,见文首),但 v3.2 本身 `stopped, NOT converged`,见上方三条明账;仓内与桌面的 `v3.1` 均保留未删(见文首收尾待办);② 本仓无可执行测试面,规则一致性只能靠外部审查兜底——v3.2 迁移的覆盖检查是**带负向对照的 grep 断言**(有区分力、可复现),但它只证明"机制锚点在场",不证明"叙述与母本语义等价",后者仍无机械验证手段;③ `b5cbb9d` 那一行的关闭状态未经第 6 轮独立确认(可机械 grep 确认:必含类别全仓只剩 `AGENTS.md:158` 一处)。
* **四个停滞任务未合并**:各自 HANDOFF 均记录当前不可合并(「Author 不得标 Ready to Commit」/「双审通过后才 merge」而双审未过 / 任务进行中 / 6 条 blocking 未修复已移交),待人类逐个裁决「带如实登记的限制交付 vs 按新预算重拆」。

## 状态(2026-08-12 更新)
* **Commit B 落地(2026-08-07)**:Reviewer 调用形态收敛入 `claude/workflow/reviewer-prompt.md` 双审隔离协议 ③(唯一定义处)——显式 sandbox/model/效力档 + `--ephemeral --ignore-user-config --ignore-rules` + 机器键 `-c windows.sandbox="elevated"`(缺它则全命令被 policy 拒,冒烟实测)。**read-only 未晋升**:六轮行为冒烟证实明文 HTTP 出网在两种沙箱模式下均放行(实拉真页面 200/559B),「网络阻断」验收不成立——继续显式 workspace-write(fallback 形态已在一次性 fixture 仓行为验证);沙箱≠网络边界已记为已知风险,复测晋升需人类明确决定。
* **Commit A + mini A/B 结案(2026-08-06,Complete)**:Routine/Critical 模式路由已落地并部署(`3925b5c` 主体 + `83985f5` 措辞修正);Routine 减重与 Critical 完整性均有**行为级**证据;复发即重开裁决,不得静默。全档:`archive/2026-08-06-routine-critical-routing/HANDOFF.md`。
* **H5A validator:停牌封存**(`stopped, NOT converged — over-engineered`,人类裁决)。代码留在 `tools/validate/`(Common + PathReferences + 81 测试):非门禁、勿续建、勿修;`validate.ps1` 入口从未建成;真仓断言绑定修漂移前的仓库状态,套件对当前 main **预期失败**——这是封存标记,不是 bug。全部过程、裁决与账目①–㉒:`archive/2026-08-05-h5a-validator-foundation-stopped/HANDOFF.md`。
* **已知漂移已直接修复**:install.sh/skills 幽灵引用、push 指令冲突(b13859d);根 LICENSE(MIT)+ THIRD_PARTY_NOTICES 含 ECC 上游原文(a5802cf);ecc marketplace 残留与可执行恢复指令清除(78cb432)。
* **IMPROVEMENT_PLAN v1.1 = 参考材料(REFERENCE ONLY)**,不按其 phase 开工;重启任何一项需人类明确决定。
* **部署**:`install.ps1` 受迁移期 guard 锁定,勿直接运行;部署 = 从 main 精确同步受管文件 + 哈希比对。**受管持续镜像面(边界唯一定义处 = README 文首 + `AUTHORITY_CONTRACT.md`)** = `claude/{CLAUDE.md,settings.json,rules/,workflow/,commands/}`(98 文件)+ `codex/AGENTS.md`;`codex/config.example.toml` 为 seed-only(缺失时播种、创建后 machine-local),**不参与同步**。审计史:2026-08-07 的 blob 级审计**范围仅 `claude/**`**(检出 67 文件 EOL-only 分歧,已双侧规范化为 LF、内容级零变化;更早:2026-08-05 15/15、2026-08-06 2/2);**2026-08-12 首次把 `codex/AGENTS.md` 纳入审计**——检出本机多 8 行 H5A 时代过期条款(change budget / 快速版 code-review 暂停 fail-safe,正是 Codex 误判该类流程要求的根源),备份后 mirror-replace(101→93 行),**两面现均 blob 级零漂移**。教训:审计范围须覆盖两个 agent 的受管面。**2026-08-15(v3.2 落地后)**:全受管面 blob 级审计 + 精确同步——审计前 98 同 / 1 异 / 0 缺(唯一差异 = 本次改的 `claude/CLAUDE.md` 便携版指针),备份 `~/.claude/CLAUDE.md.bak-20260815-155904` 后同步该 1 文件;**同步后复审计 99/99 一致、零漂移**。同批确认 `~/.claude/workflow/archive/**` 下 **82 个仅本机文件**(双审隔离演练、change-budget 与守护契约 pre/post 备份等历史取证)**完好未动**——故本次走「精确同步受管文件」而非 mirror-replace,后者会按 README 警告删掉这 82 个。`portable/` 不属受管镜像面,桌面副本单独核对哈希一致。

## 工作方式(五规则三闸门,裁决㉒)
日常改动 = Claude 改 + 人类扫 diff + 人类 commit/merge。五条核心规则:快照仓 SSOT / 真改动人类批准 / Author 交真实测试产物 / Reviewer 零写入只读 diff 与证据 / 连续 blocking 硬停拆新任务(判据唯一定义处 = `claude/workflow/AGENTS.md` → Fix-Loop 计数与跨轮硬停,此处不复述)。三个机械闸门:审查 SHA 绑定干净工作树、实际 diff 不超批准范围、测试真实执行退出码可信。9P 计划审、9A/9B 双审等重流程仅用于人类明确要求的真项目;**任何新增流程/规则/登记表/检查项默认「不」**,除非一句话说清净收益。模式路由(Routine 默认/Critical 人类启用)唯一出处:全局 `CLAUDE.md` → Mode Routing。
