# last_test_run.txt — task/dsh-landing（DSH 落地）

收集时间：2026-09-06 · 收集者：Author · **绑定 `tested_sha = a361bc1942546484e0edd102cbd789d4dc498c61`**
说明：本任务**没有功能测试套件**（改的是文档 + 一个受 guard 锁定的安装脚本）。下列均为机制核验，命令与真实输出照抄；失败/未跑项如实标注。

---

## A. 本任务产出物的一致性（Author 自跑）

```
$ git -C <repo> rev-parse HEAD
a361bc1942546484e0edd102cbd789d4dc498c61

$ git -C <repo> status --porcelain
(空)

$ git -C <repo> status --porcelain --ignored | Select-String -Pattern '9A|9B|review_|_raw\.log'
(空)

$ # 仓内 dsh/** 与部署副本 ~/.dsh 逐文件 SHA256 比对
compared=23 mismatches=0

$ git diff --no-index --numstat claude/workflow/reviewer-prompt.md dsh/workflow/reviewer-prompt.md
100     36      {claude => dsh}/workflow/reviewer-prompt.md
```

结论：快照干净、部署副本与仓内全等；`reviewer-prompt.md` 的变更量为 100 增 / 36 删（远超"12 处有界改点"的登记面 → 印证 9A 的 B2）。

---

## B. 证伪 9A/9B 的 B1（`reasoning_effort: medium` 不存在）

```
$ $raw = Get-Content "$npmCache\@deepseek-ai\dsh-llm-deepseek\lib\index.js" -Raw
$ $i = $raw.IndexOf('function reasoningEffort(effort)'); $raw.Substring($i, 260)
function reasoningEffort(effort) {
        if (effort === "off" || effort === "low" || effort === "high" || effort === "max") return effort;
        throw new LlmError(`DeepSeek does not support reasoning effort "${effort}"`, "UNSUPPORTED_REASONING_EFFORT");
}
/** Resolve o

$ Select-String -Path <同上> -Pattern '"medium"'
(零命中 —— 退出码 1)
```

结论：**适配器取值域 = off / low / high / max，`medium` 不存在**。`dsh/workflow/reviewer-prompt.md`、`fanout-toolchain.md`、`skills/independent-review/SKILL.md`、`portable/通用prompt-DSH-v1.txt`、`README.md` 共 11 处把 9P 写成 `medium` → **9P 调用必然失败**。9A/9B 的 B1 **成立**。
（`claude/workflow/reviewer-prompt.md` 与 `portable/通用prompt-v3.8.txt` 里的 `medium` 属 Codex 侧 `model_reasoning_effort`，那里合法，**不在本任务修复范围**。）

---

## C. 证伪 9A 的 B3（`install.ps1` 注释 vs 代码）

```
$ (Get-ChildItem -Recurse dsh\skills | Where-Object Name -like 'phase-*').Count
0

$ Select-String -Path install.ps1 -Pattern 'phase-'
16: # files whose name starts with 'phase-' are mirror-replaced (they are the workflow's
```

结论：**注释描述的文件名不存在**（`phase-` 只出现在 phase 文件的 frontmatter `name:` 字段里），而代码按目录整树 mirror-replace 两个 bundle → 注释低报/错报爆炸半径。B3 **成立**。

---

## D. 证伪 9A 的 B4 / 9B 的 B2(ii)（headless 真实 CLI 面）

```
$ Select-String -Path "$npmCache\@deepseek-ai\dsh-headless\lib\startup.js" -Pattern 'helpOption|argument\(|name\('
21: return new Command().name("dsh --profile headless")
      .description("Answer one task, stream reasoning to stderr, print the final assistant message, and exit.")
      .helpOption("-h, --help", "show this help")
      .argument("[task...]", "the task text; multiple words are joined by spaces")
```

结论：headless 只接受 `-h/--help` 与位置参数 `[task...]` → **无 `-o`、无 `--model`/`--provider`/推理档参数**。`dsh/workflow/AGENTS.md:148` 的"由 Author 启动的 `-o`（headless 路径）"是**跑不通的指令**（B4 成立），且备用路径**钉不住模型档**（9B B2(ii) 成立）。

---

## E. 逐字一致审计（Author 自跑，17 对派生文件）

```
dsh\workflow\AGENTS.md            ->  25     15
dsh\workflow\reviewer-prompt.md   -> 100     36
dsh\workflow\QUALITY_GATES.md     ->   3      3
dsh\workflow\index.md             ->  20     20
dsh\workflow\workflow-design-notes.md -> 1    1
dsh\workflow\AB-model-diagnostic.md   -> 0 (逐字节相同)
phases\debug.md          ->  4   2      phases\design-check.md -> 3  1
phases\define.md         ->  4   2      phases\explore.md      -> 3  1
phases\final-review.md   ->  6   4      phases\implement.md    -> 5  3
phases\plan.md           ->  8   5
templates\{HANDOFF,IMPLEMENTATION_PLAN,PRODUCT_BRIEF,TASK_BRIEF}.md -> 0 (逐字节相同)
```

```
$ # QUALITY_GATES 指针漂移
$ (Select-String claude\workflow\reviewer-prompt.md -Pattern 'docs/ai/QUALITY_GATES\.md').Count
3
$ (Select-String dsh\workflow\reviewer-prompt.md -Pattern 'docs/ai/QUALITY_GATES\.md').Count
1
$ (Select-String dsh\workflow\reviewer-prompt.md -Pattern '~/\.dsh/workflow/QUALITY_GATES\.md').Count
2
```

结论：**`docs/ai/QUALITY_GATES.md` 的指针有 2 处被机械替换成了母本路径**（9B B3 成立）——这是我做全局 `~/.claude` → `~/.dsh` 替换时**没有排除 `docs/ai/QUALITY_GATES.md` 这个看起来同形的字符串**造成的。同时 `dsh/workflow/AGENTS.md` 的 25+15 明显大于登记面（9A B2 成立）。

---

## F. 安装器与技能的机械核验

```
$ [System.Management.Automation.Language.Parser]::ParseFile('install.ps1', [ref]$t, [ref]$e); $e.Count
0        # 语法解析 0 错误（尚未运行；脚本受迁移期 guard 锁定）

$ # skill frontmatter：两个 SKILL.md 含 name/description；phase 文件额外含 disable-model-invocation: true
$ # 活证据：本会话技能目录只剩两项 dual-agent-workflow / independent-review
$ #          （references/** 未被当作技能 → 与 dsh-skill-filesystem 的"嵌套 **/SKILL.md 不被发现"一致）
```

结论：解析与技能发现面成立；`install.ps1` 的**运行行为**未验证（guard 锁定，H3 后需实跑，见 9A VN3）。

---

## G. 未跑 / 未验证（如实登记，不得当作通过）

* **未跑 9P 真实调用**（B1 修复后必须补一次实跑取证：`reasoning_effort` 用修复后的取值）。
* **未跑 `dsh --profile headless` 的完整审查轮**（只跑过两次"可用性/模型 id"冒烟，见 `docs/ai/DSH-LANDING-NOTES.md` §5）。
* **未运行 `install.ps1`**（受迁移期 guard 锁定；AGENTS.md 明令勿直接运行）。
* **未做 `subagent` 端到端审查取证**：本轮两次 9A/9B 调用**是**真实发生的（返回正文已落账），但**未实测** `medium` 被拒的完整错误报文（仅静态证据）——若要坐实，见 9A 的 VN1。
* 本任务**无功能测试套件**，故不存在"全量测试退出码"这类证据。

---

# 第 2 轮：Reviewer 的 Verification Needed 代跑（2026-09-06）

收集者：Author · **绑定 `tested_sha = 7084fb7515ef36e8191f20a141bf2118cd20ff4e`**（第 2 轮审 tip）
说明：本文件上方的 A–G 节绑定的是**第 1 轮**的 `tested_sha = a361bc19`（修补前），**保留不删**以便对照；下方为第 2 轮代跑结果，逐条对应两份 verdict 的 VN。

## H. VN1（9A/9B 共同要求）：9P 的 `high` 档位实跑取证

命令（真实执行：前台 `subagent`，参数逐字为该值）：

```
subagent(description: "9P effort probe", run_in_background: false,
         provider: "deepseek-official", model: "deepseek-flash",
         reasoning_effort: "high",
         prompt: "档位连通性探针…只回三行，不要使用任何工具")
```

真实返回（照抄）：

```
model: deepseek-flash（据全局指令记录；我无法自证运行时配置）
effort: 无法自证（未收到任何档位声明）
status: OK
```

**结论**：调用**未被拒**（第 1 轮 B1 的病征是"必然被 `UNSUPPORTED_REASONING_EFFORT` 拒绝"，此处未出现）→ **9P 门在当前配置下可跑**。
**如实标注两点**：① 这只是一次**连通性探针**，不是真实 9P 审查轮；② 子 agent **无法自证**自己的 provider/model/effort——这正是 `model_route` 只能是"自报值、非证据"的实证。

## I. VN2（9A）+ AC4 判定与负向对照实跑（9B 的 VN4）

判定命令（AC4 Amendment 后的形态：范围 = DSH 面）：

```
$files = (Get-ChildItem dsh -Recurse -File -Include *.md,*.txt).FullName + <portable/通用prompt-DSH-v1.txt>
$vals  = 对每个文件扫 reasoning_effort 赋值位取值
$vals | Where-Object { $_ -notin @('off','low','high','max') }   # 越界集必须为空
```

正向（当前 tip）真实输出：

```
取值集合: high
越界: (空)
```

适配器侧：

```
$ Select-String -Path <@deepseek-ai/dsh-llm-deepseek>/lib/index.js -Pattern '"medium"'
命中数 = 0
```

**负向对照（必须实际红过才算有区分力）**：把 `dsh/workflow/fanout-toolchain.md` 的 `reasoning_effort: "high"` 临时改成 `"medium"`，重跑上述判定：

```
变异后取值: medium  →  越界: medium
判定结果: RED (有区分力)
```

随后还原，并确认工作树干净（还原后 `git status --porcelain` 无该文件条目）。→ **AC4 判定成立（可复现 + 有区分力）。**

## J. VN3（9A VN2）：备用路径 CLI 面实跑

```
$ npx -y @deepseek-ai/dsh --profile headless --help
Usage: dsh --profile headless [options] [task...]

Answer one task, stream reasoning to stderr, print the final assistant message,
and exit.

Arguments:
  task        the task text; multiple words are joined by spaces

Options:
  -h, --help  show this help

Examples:
  dsh --profile headless "run the tests"     answer one task and exit
exit=0
```

**结论**：实测确认 headless **只有 `-h/--help` 与位置参数 `[task...]`**——无 `-o`、无 `--provider`/`--model`/推理档参数。AC3 关于"备用路径 CLI 面"的判定**成立**；同时坐实"备用路径钉不住路由"这条事实（9B 的 B2(ii)）。

## K. 运行副本一致性（9A 独立复核过；此处留 Author 侧产物）

```
$ # 仓内 dsh/** vs ~/.dsh 逐文件 SHA256
compared=23 mismatches=0
```

**时点**：第 2 轮修补后再同步，发生在第 2 轮审查窗口**开启之前**（9A 以 mtime 20:21 < commit 20:22 判定"无窗口污染"）。本轮（第 3 轮 delta）**尚未再同步**——见下方未验证项。

## L. 仍未跑 / 仍未验证（不得当作通过）

* **未跑完整的 headless 审查轮**（只跑了 `--help` 与一次可用性冒烟）→ 9B 的 VN5 未做。
* **未运行 `install.ps1`**（迁移期 guard 锁定）→ AC8 仍只有静态核验。
* **未做 `~/.dsh` 的机器态哈希基线比对**（AC8 要求的三项：`settings.yaml` / `sessions/` 计数 / 凭据存在性）。
* **第 3 轮 delta 未再同步到 `~/.dsh`**，也未重审。
* **`model_route` 的真实性无机械手段可证**——它是自报值。
* **本任务无功能测试套件**，不存在"全量测试退出码"这类证据。

---

# 第 3 轮：Reviewer 的 Verification Needed 代跑（2026-09-06）

收集者：Author · **绑定 `tested_sha = 34b60370fdac02b3d2670a0058b729bc99a7589f`**（第 3 轮审 tip）

## M. VN1（9A 判为"最高优先"，9B 的 VN-1 同源）：`medium` 被拒的**负向对照**——首次实跑

命令（真实执行：前台 `subagent`，`reasoning_effort` 取**不存在的档位**）：

```
subagent(description: "medium rejection probe", run_in_background: false,
         provider: "deepseek-official", model: "deepseek-flash",
         reasoning_effort: "medium",
         prompt: "若你收到本消息并成功启动，说明本调用未被拒绝。请只回一行：PROBE-REACHED")
```

真实返回（照抄，这就是完整报错）：

```
Error: provider "deepseek-official" model "deepseek-flash" does not support reasoning effort "medium"
```

**结论**：**子 agent 未被创建，调用在预检即被拒**。文档三处（`reviewer-prompt.md:51`、`fanout-toolchain.md:33`、`portable/通用prompt-DSH-v1.txt:166` 语境）关于"写 `medium` 会抛 `UNSUPPORTED_REASONING_EFFORT`"的声称，由**静态 grep** 升级为**有原始报错原文的实跑证据**；同时它构成 `medium` 那条 negative control 的**完整对照对**（`high` 成功返回探针 + `medium` 被拒 → 机制存在且由该取值触发）。
**如实标注**：报错文案与本文件旧稿写的错误码字面（`UNSUPPORTED_REASONING_EFFORT`）不完全一致（实际报文不含该码）；The 文档目前引用的是错误码，**建议保留错误码并附带这句原文**，或改为引用原文。

## N. VN4（9A）：9P `high` 的"未被拒"与档位送达

§H 的探针（`high`）**成功返回**，§M 的探针（`medium`）**被拒**——两者对照已能说明"该取值被适配器校验且 `high` 通过"。但**"值确已送达并生效"仍无法自证**：两份探针的子 agent 都声明无法读自己的运行时配置。
**处置（采纳 9A 建议）**：把 §H 的结论**收窄**为"`high` 未被拒、`medium` 被拒（§M）；档位是否实际生效无机械证据"。`model_route` 仍是自报值。

## O. 第 3 轮 tip 的机制核验

```
$ # skill frontmatter / 手册指针 / AC4 判定（DSH 面）
取值集合: high
越界: (空)
适配器 'medium' 命中数 = 0
$ # 仓内 dsh/** vs ~/.dsh
compared=23 mismatches=0        （第 3 轮 delta 已在审查窗口开启前同步）
```
**时点更正**：本文件 §K/§L 曾写"第 3 轮 delta 未再同步"——该句写于 `956db71` 时点，`34b6037` 提交时已同步；**两份第 3 轮 verdict 都独立实测为 23/23 全等**。此句在此更正，§K/§L 保留原文以便对照。

## P. 仍未跑 / 仍未验证（不得当作通过）

* **真实 9P 审查轮**（两次探针都不是审查轮）——9B 的 VN-2。
* **`settings.yaml` 的 `reasoningEffort` 效果**（只有 schema 层证据）——9B 的 VN-3。
* **AC6 判定重跑**（B1 修复后必须留产物）——9B 的 VN-4。
* **AC8 机器态实跑**（临时 HOME 下跑 `install.ps1` + 三项哈希比对）——9B 的 VN-5。
* **headless 完整审查轮**——9B 的 VN-7。
* **AC4 负向对照的"字面命令 + 退出码"产物**：§I 为转述；本轮 §M 提供了另一条更强的对照（真实报错），但 §I 的字面命令仍缺。

---

# 第 4 轮：AC6 判定重做（人类裁决 9B 的 Product Blocking 成立）

收集者：Author · 人类裁决（2026-09-06）：**9B 第 3 轮的 Product Blocking 归类成立**，修完再跑一轮。

## Q. AC6：把"不可判定"改成"可判定"（含三条负向对照）

**问题（第 3 轮 9B 的 B1）**：AC6 原判定谓词是"文件名是否出现在 `DSH-LANDING-NOTES.md` 里"——这个谓词**没有区分力**：正文别处任何一次提及都会匹配。实测：正向看似通过，但**两个负向对照都绿**（删掉 §2.2 的声明行、或删掉 portable 的登记行，判定照样 GREEN）。
**根因两条**：① 谓词形态错（自由文本匹配）；② `git diff --name-only` 默认 `core.quotepath=true`，把非 ASCII 路径转义成 `\351\200\232…`，与登记表里的 `通用prompt-DSH-v1.txt` **永远不相等** → 假红。
**处置**：在 `DSH-LANDING-NOTES.md` 增设 **§2.3 机读登记表**（`- path: <仓库相对路径>`，30 条），AC6 的谓词改为**直接读 §2.3**并双向比对。

判定命令（与 `TASK_BRIEF.md` AC6 逐字一致）：

```powershell
$base = 'bf06c65d0831ebeb2b0982f35ae2d7f4c65c2c17'
$notes = Get-Content docs/ai/DSH-LANDING-NOTES.md -Raw
$reg = [regex]::Matches($notes, '(?m)^- path:\s*(\S+)\s*$') | ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique
$scope = git -c core.quotepath=false diff --name-only "$base..HEAD" -- dsh portable install.ps1 AGENTS.md README.md docs/ai/AUTHORITY_CONTRACT.md docs/ai/DSH-LANDING-NOTES.md docs/ai/TASK_BRIEF.md | Sort-Object -Unique
$missing = $scope | Where-Object { $_ -notin $reg }
$stale   = $reg   | Where-Object { $_ -notin $scope }
if ($missing -or $stale) { $missing; $stale; exit 1 } else { 'AC6: register == scope'; exit 0 }
```

**正向真实输出**：
```
reg=30  scope=30
register == scope → GREEN (exit 0)
```

**三条负向对照（每次只改一个变量，实测全红且抓到的正是被变异项）**：

| # | 变异 | 结果 |
|---|---|---|
| NC1 | 删掉 §2.3 里 `- path: dsh/workflow/templates/HANDOFF.md` 那一行 | **RED**，`$missing` = `dsh/workflow/templates/HANDOFF.md` ✅ |
| NC2 | 把 `- path: portable/通用prompt-DSH-v1.txt` 改成 `…-vX.txt` | **RED**，抓到 `portable/通用prompt-DSH-v1.txt`（missing）与 `…-vX.txt`（stale）✅ |
| NC3 | 加一条不存在的登记 `- path: does/not/exist.md`（反向） | **RED**，`$stale` = `does/not/exist.md` ✅ |

每次变异后均还原，并核 `git status --porcelain -- docs/ai/DSH-LANDING-NOTES.md` 回到仅有本轮正常改动（无遗留变异）。

**结论**：AC6 从"按字面恒红且不可复现"变为**可复现 + 有区分力**；第 3 轮 9B 的 B1 所指缺陷已闭合。

## R. 本轮其余修补（对应第 3 轮两份 verdict 的其余 Suggestion）

* AC1 补判定方式（对象 = 单文件对；**第 3 轮 9A 已全量核验 40 行、结论"无漂移"**）；并纠正 Amendment 里"17 对/2600+ 行"的**张冠李戴**（那是 AC10 首句的活）。
* AC4 的"冻结输入域"旧节改写为**指针**（旧域按字面会因 Codex 侧 `medium` 假红）。
* AC9 计数"三笔"→"**四笔**"并补判定方式（被禁措辞零命中 + 与 `[DEBT]` 清单对应）。
* AC10 判定从"指针行数相等"（对声称无区分力）改为**逐对 `--numstat` + 每行归入某改点**。
* `DSH-LANDING-NOTES` §2 第 10 条的"只改了末尾换行"**事实订正**（`explore.md` 仅 frontmatter 两行；`define.md`/`design-check.md` 另含路径改写）。
* README 快照节的 DSH 状态块、`DSH-LANDING-NOTES` 状态块、债 #1/#4 台账按轮次改写。

## S. 仍未跑 / 仍未验证（不得当作通过）

* 真实 9P **审查轮**（两次都只是探针）。
* `settings.yaml` 的 `reasoningEffort` 实际生效证据（只有 schema 层）。
* AC8 机器态实跑（临时 HOME 下跑安装器 + 三项哈希）。
* headless 完整审查轮。
* AC4 负向对照的**字面命令 + 退出码**产物（§I 是转述；§M 提供了更强的真实报错对照）。

---

# 第 4 轮：双审结果与代跑（2026-09-06）

收集者：Author · 人类裁决（2026-09-06）：采纳第 3 轮 9B 的归类（AC6 是 Product Blocking，成立），"修完再跑一轮"。
`review_base_sha = 34b60370` → `review_tip_sha = fc32899f`

## T. 两份 verdict 的结论

| | 9B 盲审 | 9A 标准审 |
|---|---|---|
| Review Verdict | **不通过** | **有条件通过** |
| Blocking | **1 条**（`caused_by_last_fix: dispute`） | **None** |
| Suggestion | 12 | 11（R4-S1…S11） |
| Debt | Unpaid | Unpaid |

**上轮 B1（AC6 按字面恒红）两份都独立实跑确认已闭合**：AC6 谓词在 tip 上 `reg == scope == 30` → GREEN；base 已钉死为 `bf60c65`（`bf06c65`）；`core.quotepath=false` 的必要性也被两份各自复现。

## U. 本轮新暴露的缺陷：**我自己新写的判定，有两条是坏的**

这是本轮最重要的发现，且**两份 verdict 独立命中同一族**：

1. **AC6 的第二条反例按字面恒绿、且从未执行**（9B 的 NS-1 = 9A 的 R4-S1）：`TASK_BRIEF` 写"把 **§2.2** 任一模板声明行改成不存在的文件名 → 判定必须红"，但 §2.2 是 **markdown 表格**（行首 `|`），而谓词正则只认 `^- path:` 行（30 条全在 §2.3）→ **该反例不可能变红**。真正跑过的 NC1 删的是 §2.3 的行——**文字里写的反例与真正跑过的不是同一个**。9A 在内存里复现：`reg=30 missing=0 stale=0 → GREEN`。
2. **AC9 判定方式①（"被禁模糊措辞零命中扫描"）未定义 scope，按最自然范围读必然恒红**（9B 的 B-1 = 9A 的 R4-S4）：交付面里本来就含**禁令原文引用**（`dsh/workflow/AGENTS.md` 的词表、`conflict-hard-stop.md`、portable、甚至 `TASK_BRIEF` 自己）→ 实测 10 处命中。**恒红项还是母本判据本身**；想让变绿就得删改判据行，而那会同时触发 AC1/AC10 的红集合——AC9① 与 AC1/AC10 **互斥**。
3. 附带（均为 Suggestion 级）：AC9 判定②要求的"与 `[DEBT]` 清单逐条对应"**两集合零交集**（9A R4-S5）；AC10 点名 `--numstat` 但该命令**不输出变更行**，支撑不了它自己的谓词（9A R4-S6）；`TASK_BRIEF:58` 的"`dsh/workflow/` 不在 scope 里"**与事实相反**（scope 的 `dsh` 覆盖它，实测 30 项里 22 项在 `dsh/` 下，9B NS-2 = 9A R4-S3）；`§2.3` **不是真标题**（粘在 §2.2 括注行尾，9B NS-3）。

**诚实记账**：本轮修好了第 3 轮那条 Product Blocking，**同时在同一个 Amendment 里写坏了另一条（AC9）并把 AC6 的反例指向了错误的节**。这与 9B 的 Requirement-Level Concern 一致——三轮的 finding 全部落在"登记表 + 验收条款"这一层，**而本轮的修复又在这同一层新增了新的不可执行判定**。

## V. 两份独立确认的**谓词能力上限**（不构成缺陷，但必须收窄声称）

9B（NS-12）与 9A（Test Coverage Gaps）各自独立给出同一个纸面反例并各自判定"Author 的三条负向对照否不掉它"：
**AC6 的谓词只有"路径级"区分力**——在**已登记路径内部**做未登记的内容改动（例：往 `dsh/workflow/index.md` 加一条未登记进 §2 #9 的判据行），`$scope`/`$reg` 都不变 → 判定仍 GREEN。
**Author 确认该判断正确**：NC1–NC3 全部只变异登记表侧，没有一条从文件内容侧下手。**AC6 首句"覆盖全部实际变更"是过度声称**，应收窄为"覆盖交付面的**路径**集合"。

## W. 仍未跑 / 仍未验证

* **AC9 判定①/② 与 AC6 第二条反例：本轮无任何产物**（前者恒红、后者恒绿，两者都不可执行）。
* **AC6 三条负向对照只有摘要表**，缺命令原文级输出与退出码（9B VN-3、9A Test Coverage Gaps）。
* **VN-B（9A 提出，Author 无法自证）**：`TASK_BRIEF:46` 与本文 §R 引用"第 3 轮 9A 已全量核验 40 行、结论无漂移"——9A 本轮**不能打开** `review_9A_r3.md` 核实这句话是否真由 9A 说过。**请人类打开 `docs/ai/review_9A_r3.md` 确认**；若没有，则该引用属"把未做的核验写成已做"，须按 AC9 处置。
* 真实 9P 审查轮、`settings.yaml` 档位生效、AC8 机器态实跑、headless 完整审查轮、AC4 字面命令产物（§M 是更强的真实报错对照，但替代不了 AC4 自己写的"退出码非 0"）——见 §S。

---

# 第 5 轮（收口轮）：按人类裁决把 AC 分三级、只留两个机械门

人类裁决（2026-09-06）：**按 Author 建议收口**——不再在"登记表 + 验收条款判定方式"这一层续修，改成给每条 AC 定级。

## X. 收口内容

**新增判据白名单（封闭）**：`[M] 机械门`（命令 + 退出码 + 已实跑的负向对照）/ `[O] 单点观测`（只声称"做过一次并留了产物"，不推广为"不存在"）/ `[U] 未验证`（如实登记 + 触发时机，不是债）。
**只保留两个机械门**：**AC6**（改点登记）与 **AC4-门**（档位取值域）。**其余 AC 一律降为 [O] 或 [U]，并当场收窄声称。**

| AC | 原形态 | 收口后 |
|---|---|---|
| AC1 判据一致 | "逐条一致"（散文对读） | **[O]**，只覆盖 `AGENTS.md` 那一对（两轮 9A 逐行核过 40 行） |
| AC2 skill 可加载 | 有观测 | **[O]**（本机一次观测） |
| AC3 两条路径 | 混合 | **[O]**（主路径 8 次真实调用 + headless `--help`）/ **[U]**（备用路径完整审查轮） |
| AC4 档位 | 判定命令是伪码 | **[M]**，见下 §Y |
| AC5 零写入 | 有观测 | **[O]**（8 份 verdict 全 `none`；自证字段、无机械手段） |
| AC6 改点登记 | [M] | **[M]**，声称收窄为"**路径级**覆盖" |
| AC7 说与做一致 | 散文 | **[O]**，只核备份/镜像语义一组 |
| AC8 机器态 | "判定方式"要求实跑 | **[U]** + 触发时机（脚本 guard 锁定，从未运行） |
| AC9 如实标注 | 两条坏判定 | **[O]**——**两条原判定（措辞零命中扫描 / 与 `[DEBT]` 逐条对应）如实登记为已废弃**，改为人工单点读 |
| AC10 判据无漂移 | `--numstat`（给不出变更行） | **[O]**，改用 `-U0`，且**只覆盖与 AC1 同一对**；**停止在本文件复述指针处数** |
| AC11 未验证清单 | 散落 | **[U] 汇总**（6 项，各附触发时机），要求与 `NOTES §5` 逐条一致 |

## Y. AC4 从"伪码判定"升级为真机械门

脚本入库：`tools/ac4-reasoning-effort-check.ps1`。**真实运行结果**：

```
=== 正向 ===
adapter            = ...\_npx\1e7f6d9597241db0\node_modules\@deepseek-ai\dsh-llm-deepseek\lib\index.js
DSH-side values    = high
out-of-domain      = (none)
adapter medium hits= 0
AC4: PASS
exit=0

=== 负向对照：把 dsh/workflow/fanout-toolchain.md 的 high 改成 medium（只改一个变量）===
DSH-side values    = high, medium
out-of-domain      = medium
adapter medium hits= 0
AC4: FAIL
exit=1
（还原后工作树该文件干净）
```

→ **有区分力**；AC4 至此才第一次同时拥有"可复制命令 + 退出码 + 已实跑的负向对照"。

## Z. 收口后两个门的复跑

```
AC4: PASS   exit=0
AC6: reg=30 scope=30 → GREEN
install.ps1 解析: parse OK
```

**AC6 登记表零漂移**：本收口轮改了 `README.md` / `docs/ai/AUTHORITY_CONTRACT.md` / `docs/ai/DSH-LANDING-NOTES.md` / `docs/ai/TASK_BRIEF.md` / `install.ps1` / 新增 `tools/ac4-reasoning-effort-check.ps1`，**其中 `tools/ac4-reasoning-effort-check.ps1` 不在 AC6 的 scope 内、也不在登记表内**（第 5 轮 9A 的 B-1）→ 本句此前写"全部已登记"是**假声称**，已订正；`missing`/`stale` 均空只对 scope 内的那些文件成立。

## AA. 本轮同时修掉的第 4 轮残留（属 Suggestion 级，一并收掉）

* **§2.3 标题曾粘在上一句行尾**（不是真标题）→ 已分行；`docs/ 行` 与 `--check` 两处草稿残留已改。
* **§2.2 尾注"本节编号被 AC6 直接引用"** → 改为"AC6 现在只引用 §2.3；§2.2 改编号不再改验收基线"。
* **§2 第 10 条对 `explore.md` 的归一化描述** → 按 `--numstat` 实测改写（2 增 0 删、只有 frontmatter 两行、**无末尾换行变更**）。
* **`README:36` / `NOTES:62` 的"5 项"** → 未单独改（它们不在本轮 AC 收口的判定面内）；**已在 AC11 与 HANDOFF 记为本轮未处理项**，留待下一次触碰该文件时顺手改。
* **`install.ps1` 三处口径** → 已改（`:21`/`:77` "never touched" 改为"never modified or deleted + 见下方 WARNING"；`:97` 的 "added only when missing" 删掉）。注：`tools/ac4-reasoning-effort-check.ps1` 因含 PowerShell here-string，曾被写成 CRLF，已归一回 LF。

## AB. 仍未跑 / 仍未验证（本清单与 `TASK_BRIEF` 的 AC11 必须逐条一致）

1. 除 `AGENTS.md` 外的其余派生对是否存在判据漂移。
2. 备用路径（headless）完整审查轮。
3. 真实 9P 审查轮（两次都只是档位探针）。
4. `~/.dsh/settings.yaml` 的 `reasoningEffort` 是否真被适配器读取。
5. AC8 的机器态实跑（安装器 guard 锁定）。
6. `install.ps1` 的其余注释逐句对读。

---

# 第 5 轮：收口轮 9A 单审结果 → 硬停触发

`review_base_sha = 1c40e7a` → `review_tip_sha = 3f445b8a`（单审，9B 不参与：delta 全是 AC 文本与登记表修正）

## AC. Verdict：不通过 —— 3 条 Product Blocking

| 条 | 内容 | 归因 | Author 独立复核 |
|---|---|---|---|
| B-1 | AC6 的 scope 与登记表都不覆盖本轮新增的 `tools/ac4-reasoning-effort-check.ps1`，门却报 GREEN；且本文件 §Z 断言"全部已在 §2.1/§2.3 登记"是假声称 | yes（两套定义一致） | 复现：`git diff bf06c65..HEAD -- tools` → 该文件；NOTES 里 `tools/|ac4` 零命中；scope 不含 `tools` |
| B-2 | AC4-门只枚举它声明输入域内 20 处 `reasoning_effort` 站点中的 7 处；域外取值写成反引号/散文/大写即可走 PASS，Author 记录的负向对照否不掉它 | dispute | 复现：域内字面出现 20 处；脚本正则只捕获未加引号小写形态。**（订正指针，第 6 轮）**本条转录的"大写"一项**已被实测否掉**——真逃逸形态是驼峰键与反引号值；以 §AH 与 HANDOFF 的 `[DEBT]` 为准 |
| B-3 | 本轮 commit 把 `docs/ai/HANDOFF.md` 里所有 `@` 系统性替换成了 `x`（4 处全中），损坏 `model_route` 记录与 `[DEBT]` 触发条件 | dispute（A=yes/B=no） | 复现：base 4 行含 `@` → tip 0 行 |

streak 由 1 增至 2 → 硬停触发（B-1 为 yes）。按 AGENTS.md → Fix-Loop 三者优先级，合法出路只有回退 / 重新拆任务 / 请求人类批准架构升级；不得改走"限制交付"。

## AD. 已当场修掉的两项（不改变硬停状态）

* B-3：HANDOFF 的 5 处坏串已复原（`main @ bf06c65`、`deepseek-official/deepseek-flash@high` ×2、`@deepseek-ai/dsh` ×2）→ `@` 行数回到 4。
* B-1 的假声称部分：本文件 §Z 的"全部已登记"已订正为"`tools/…` 不在 AC6 的 scope 内、也不在登记表内"。

未修：B-1 的登记面缺口（`tools` 是否应入 scope/登记表）、B-2 的 AC4 枚举面。这两条属硬停范围内的选择，Author 不得自行择路。

## AE. 9A 对"收口"本身的评价（摘要）

* 收口大体做到了：11 条 AC 全部带 `[M]/[O]/[U]` 标记、机械门确实只有两个、`[O]` 逐条收窄了声称、`[U]` 都有触发时机且未被写成"已验证"。
* 但"不新增任何自由文本判据"这半句没做到：AC9 的新现行判定与 AC11 的镜像一致性要求都是本轮新写的自由文本判据。
* `[U]` 的定性（9A 独立判断）：实质上就是"以验证偿还的债"，与 `[DEBT]` 同族；但不构成被禁的 known-issue 变体——它是明账（两处镜像、逐项列名、各带触发时机、合并门处计数），不含被禁措辞，有明确偿还路径。唯一该改的是措辞。
* 9A 同意"9B 不参与收口轮"的形态选择，但指出 B-1/B-3 恰是"改动的副作用"型缺陷——正是盲审最容易捞到的那一类。

## AF. 一条必须写下的模式（供人类拆任务时参考）

五轮下来的分布：finding 100% 落在"改点登记表 + 验收条款判定方式"这一层；而判据层（Safety Rules / Fix-Loop / 双审隔离 / SHA 绑定 / 收敛门）经多次独立逐行核验，从未发现漂移。
且两个机械门各自也都不稳：AC6 的 scope 每一轮都跟不上下一次改动（第 3 轮 base 未钉死 → 第 4 轮声明表缺面 → 第 5 轮 `tools/` 漏面）；AC4-门只覆盖它声明域内 20 处站点中的 7 处。

---

# 第 6 轮：修 B-1（人类裁决：先只修 B-1 + 恢复 9B 跑一轮，用真实双审决定 B 能否收口）

人类 2026-09-06 裁决：走「重新拆任务」退出硬停；任务 B 的交付边界先不定，**只修 B-1、恢复 9B 跑一轮，拿到两份 verdict 后再决定**。

## AG. B-1 的修法与取证

**修法（比 9A 给的两条更彻底）**：不仅把 `tools/ac4-reasoning-effort-check.ps1` 登记进 §2.1 #25 与 §2.3，而且**把 `tools` 加进 AC6 的 scope**。理由：该脚本是 **AC4-门本身的实现**——若它既不在 scope 也不在登记表内，那么"把 `high` 从 `$allowed` 里去掉"这类**削弱第二道门的改动既不进账、也不触发 AC6**。9A 给的路线 (a)（只登记不扩 scope）会让这个洞留着：脚本被改了，scope 里却没有它。

**正向（scope 现含 `tools`）**：
```
reg=31  scope=31  →  register == scope  → GREEN (exit 0)
```
（第 5 轮是 `reg=30 scope=30`，因为当时 `tools` 既不在 scope 也不在登记表。）

**负向对照 NC-A（删掉 `tools/...` 的 §2.3 登记行，只改一个变量）**：
```
差异项 = 1  →  RED ✅ 抓到: tools/ac4-reasoning-effort-check.ps1
```

**负向对照 NC-B（削弱第二道门：把 `high` 从脚本的 `$allowed` 里去掉）**：
```
AC6 差异项 = 0  → GREEN
  （该文件已登记，故不红 —— 这正是修复的目的：它现在进账了）
AC4-门在该变异下: AC4: FAIL
```
→ **订正（2026-09-06 第 6 轮 9B 的 R6-B3）**：本句此前写"削弱第二道门现在至少会进账"是**过度声称**，且被它自己的输出证伪——NC-B 自己打在屏上的是 `AC6 差异项 = 0 → GREEN`。**AC6 是路径级谓词**（这一点 AC6 的「声称边界」已承认）：`tools/ac4-reasoning-effort-check.ps1` 一旦登记，它的**任何内容修改**都同时出现在 `$scope` 与 `$reg` 里 → 双门皆绿、**零账目信号**。同理 `TASK_BRIEF` 举的那个例子（把 `medium` 加进 `$allowed`）在当前取值下 AC4 仍 PASS。**本次修复真正新增的覆盖只有一种情形：`tools/` 下新出现一个未登记的路径会被判红。**
**第二道门的实现本身仍无机械完整性保护**——削弱它（改 `$allowed`、改正则、改 `$bad` 分支）只能靠人工读 diff 发现。已按 No-Hidden-Debt 记为 `[DEBT]`（见 HANDOFF → Remaining Risks）。
**同一声称亦出现在 `f7d88e9` 的 commit message 里**（`which also closes the hole that let the gate itself be weakened…`）——历史不可改写，**以本条订正为准**（第 6 轮 9A 的 S6）。

**还原核验**：两次变异后均还原，`git status --porcelain` 回到只剩本轮正常改动；`$allowed` 行与 `- path: tools/...` 行均确认复原。
**脚本清理（2026-09-06 第 6 轮订正）**：此前本句写"临时 `.tmp-*.ps1` 均已清"是**假声称**——承载它的那个 commit（`f7d88e9`）自己就新增了 `.tmp-r6.ps1`（删除时用了位置参数而非 `-LiteralPath`，静默失败，随后被 `git add -A` 一并提交）。该文件已在下一个 commit 中移除；第 6 轮 9B 正是因此**拒审**（`worktree_clean: no`）。**教训（2026-09-06 第 6 轮 9A 的 S5 订正；**第 7 轮再次复发，见下**）**：该归因**没有证据支撑**（两份被提交的临时脚本 `69142d9:.tmp-r5b.ps1` 与 `f7d88e9:.tmp-r6.ps1` 里**都没有 `Remove-Item`**，而**都含 `git add -A`**；r6 那份本身就是"往本文件追加一段 → `git add -A` → commit"的脚本，自身被扫入是必然而非偶然），且**防不住重演**——r5b 与 r6 正是同一模式重演了两次。**有证据的两条**：① 一次性脚本不落仓内（或落在被忽略路径）；② **不用 `git add -A`，改用具名 `git add <paths>`**。
**第 7 轮复发（这条教训的第三次被违反，说明"写下教训"本身不防复发）**：Author 在写本文件的当场又用裸 `git add -A`，把当时还在盘上的记录脚本自己扫进了索引与 HEAD 树（`git ls-tree HEAD` 命中 `.tmp-r7rec.ps1`）→ 已单独 commit 移除（`2f4e345`）。**结论：教训要落在机械形式上而不是文字上**——本任务已三次栽在同一处，而三次都不是"不知道"，是"没照做"。**可行的机械形式**：任何含 `git add` 的落账脚本，写完即由外层命令检查 `git ls-tree HEAD --name-only | Select-String '\.tmp-'` 为空；本文件即该检查的产物位置。

## AH. 本轮仍未处理（留给任务 B）

* **B-2**（AC4-门只枚举 20 处站点中的 7 处）——按人类裁决**本轮不修**。**逃逸面以实测为准（第 6 轮两份 Reviewer 各自更正过一版）**：真正静默逃逸的是**驼峰键 `reasoningEffort: <v>`**（本仓"官方"拼写）与**被反引号包裹的值**（`` reasoning_effort`: `medium` ``）；**"大写即可逃逸"不成立**（`Select-String` 默认不区分大小写 → 大写只会造成假红，更严）。以 `HANDOFF` 的对应 `[DEBT]` 为唯一出处。
* **第 4 轮那条 `dispute`**（AC9 归类）仍未裁。
* **VN-B**：`TASK_BRIEF` AC1/AC10 引用的"第 3 轮 9A 全量核验 40 行"——Author 已打开 `docs/ai/review_9A_r3.md` 核实，`:43` 确有该结论（"Reviewer 逐行读完 40 行变更、未发现判据/阈值漂移 → AC1 实质满足"）→ **引用属实**，不是"把未做写成已做"。本条从悬置清单移除。

---

# 第 7 轮（收口验证）：9A 三条收口判据 + VN-4

## AI. 9A 给出的三条收口判据，逐条核验

| 判据 | 核验命令与结果 | 结论 |
|---|---|---|
| ① 零可核查的不实条目 | PB-1（合并门条目）+ S1（逃逸面列表）+ S2（产物指针）全修；被禁模糊措辞在被改四文件上 **零命中** | **达成** |
| ② 两个门实跑绿且文本数值一致 | `AC4: PASS`（exit 0）；`AC6: reg=31 scope=31 missing=0 stale=0`（exit 0） | **达成** |
| ③ 债与未验证清单单一来源、计数自洽 | `HANDOFF` = 唯一权威台账；未验证清单 6 项在 `TASK_BRIEF` AC11 与 `NOTES §5` 逐条一致 | **订正：当时并未达成**——第 7 轮 9B 的 PB-1 实测出三处不一致：`HANDOFF` 合并门行写"6 笔"而同块有 7 条、`NOTES §3` 声称"与 `HANDOFF` 逐条一致"而两者集合差 4 项、本行却断言"计数自洽"。**已修**，见 §AP |

## AJ. VN-4：9A 的预期错了，却顺带暴露了 AC6 的一个真实盲区

9A 建议的 VN-4：「在 `tools/validate/` 下新增一个未登记路径 → AC6 应报 `$missing` 且 exit 1（确认封存档纳入 scope 不会产生假绿）」。

**实测结果：差异项 = 0 → GREEN。9A 的预期不成立。**
**原因（不是缺陷，是谓词的固有性质）**：AC6 的 scope 来自 `git -c core.quotepath=false diff --name-only "$base..HEAD"`——它比较的是**两个 commit**；`git diff` **不列出未跟踪文件**。故"新建一个未提交文件"在 scope 里根本不出现。
**真正的门是"提交 → 下一轮判红"**：新文件一旦 commit，就会出现在 `base..HEAD` 里，`$missing` 随即报它。本轮即为一例：`.tmp-r6.ps1` 被 commit 后立即被 9B 抓到（虽然它是通过快照自检而非 AC6 抓到的）。

**由此登记第 7 笔债**：AC6 只能在**提交之后**发现漏登记；"提交前自查"没有任何机械保证——本任务的 `.tmp-r6.ps1` 正是这样进过一次 tip。已写入 `HANDOFF` → Remaining Risks 与 `NOTES §3` 第 7 项。

## AK. 一条反复出现的操作性教训（记下，供后续轮次）

本任务的**核验命令自己**多次产生假结果：转义不当的 `Select-String` 模式给出假阴性（我据此一度误判"NOTES 计数未写入"）、`$?` 误用把"命中"读成"内容存在"、位置参数让 `Remove-Item` 静默失败。**规律：凡是"我声称已核验"的结论，都必须附上那条命令的原始输出；只给结论的核验在这套东西里已被证明不可信。** 这条已体现在 AC9 的 `[O]` 读点与各轮产物中。

---

# 第 11 轮：B-2 的真实风险面量化（本轮唯一的实质增量）

## AL. 为什么要重做这一项

此前对 B-2 的表述有两处不准：① 第 5 轮 9A 说"域外取值写成**大写**即可走 PASS"——**已被实测否掉**（`Select-String` 默认不区分大小写）；② 我自己在第二轮枚举差集时**读错了行号**（对 `Match` 对象取 `LineNumber`，该属性不存在，`[array][$null]` 回落到第 1 行，于是打印出一堆"```"）。**这正是本任务反复出现的同一模式：核验命令自己出错，结论就不能信。** 本轮改用 `Get-Content` 逐行 + `[regex]::Matches` 重做。

## AM. 差集的逐行分类（域内 `reasoning_effort|reasoningEffort` 共 **20 处提及 / 18 行**——**第 7 轮 9B 复跑更正：此前写的 23 是错的**；其中 **11 行含未捕获提及**）

| # | 类别 | 位置 | 它是什么 |
|---|---|---|---|
| 1 | **键名提及** | `dsh/workflow/fanout-toolchain.md:11` | 能力表里列"能指定 `provider` / `model` / `reasoning_effort`"——**不是赋值位** |
| 2 | **驼峰键** | `dsh/workflow/fanout-toolchain.md:33` | 散文式陈述取值（"三类审查统一 `high`"）；驼峰出现在同段的键名说明里 |
| 3 | **驼峰键** | `dsh/workflow/fanout-toolchain.md:50` | **记载 settings 层的键名就是 `reasoningEffort`**——属文档事实，不是赋值位 |
| 4 | **驼峰键** | `dsh/workflow/reviewer-prompt.md:51` | 同 #2：散文式陈述取值（"9A / 9B = `high`，9P = `high`"） |
| 5–9 | **占位模板** | `reviewer-prompt.md:121 / 126 / 157 / 198 / 272` | `model_route: <provider>/<model>@<reasoning_effort>` 与"证据头"描述——**占位符，不含取值** |
| 10 | **散文取值** | `portable/通用prompt-DSH-v1.txt:166` | "9A/9B = `high`，9P = `high`；可接受值 `off / low / high / max`" |
| 11 | **散文取值** | `README.md:47` | "推理档（`reasoning_effort`：9A/9B 与 9P 都是 `high`）" |

## AN. 由此得到的结论（**收窄 B-2 的严重性，且这是可证的**）

1. **未捕获共 13 处提及（分布在 11 行）**，其中 **7 处/7 行**（`fanout-toolchain:11`、`:50`、`reviewer-prompt:121/126/157/198/272`）是**键名提及或 `<…>` 占位模板**——本来就不该被枚举；把它们计入"漏覆盖"是口径错误（**第 7 轮 9B 独立复跑纠正了此处与总计数**：域内提及总数应为 **20 处 / 18 行**，不是 23）。
2. **真正"陈述取值"而未捕获的是 4 行**（`fanout-toolchain:33`、`reviewer-prompt:51`、`portable:166`、`README:47`），形态是**散文式**（`` 9A/9B = `high` ``）或与驼峰键名同段；7 + 4 = 11 行，与实测一致。
3. **而它们的取值全部是 `high`** —— 域内当前**没有任何域外取值**（`out-of-domain = (none)` 与 `adapter medium hits = 0` 两份 Reviewer 各自独立复现）。
4. **因此 B-2 的真实风险是"未来的写法可逃逸"，不是"现在有活的假绿"。** 这与 9B 的独立结论一致（它自己写："当前无活的假绿"），但比 9A 原先的描述（"域外取值写成反引号/散文/大写即可走 PASS"）**窄得多**：真正能逃逸的只有**散文式**与**驼峰式**两种写法，而已知写法里没有一个真的逃掉了。
5. **对"AC4 的 `[M]` 标记是否名不副实"的直接回答**：AC4 的**输入域声明是"赋值位"**，而仓内 `reasoning_effort:` 形式的赋值位 **7/7 全被覆盖**；未覆盖的是**非赋值位的陈述**。故严格说：**AC4 在它自己声明的域内是完整的**；名不副实之处在于"**取值还可以出现在赋值位之外的散文里，而门不看那里**"。修法因此应以**收窄声称**为主（声明门只管赋值位），而非加宽正则——除非将来确有散文式取值被写错。

## AO. 因此对 B-2 方向的建议（供人类裁决，Author 不自行选定）

* **选项 1（推荐）**：**收窄 AC4 的声称**为"只覆盖 `reasoning_effort: <未加引号小写>` 的**赋值位**；散文式与驼峰式取值不在本门覆盖内"，并保留现有两条负向对照即可。**理由**：实测证明赋值位 7/7 覆盖、无活假绿；扩正则会把"散文"纳入机器判定，而那正是这套东西六轮来反复出错的那类判据（自由的文本匹配）。
* **选项 2**：加宽正则以覆盖散文式与驼峰式，并**各补一条负向对照**。代价：散文匹配天然脆（本次枚举脚本自己就因行号读错而失败一次），且会把"文档措辞"变成门禁对象。
* 无论选哪个，`HANDOFF` 的第 6 笔债（AC4 正则只覆盖未加引号小写形态）都应按所选路线改写描述。

**本轮的量化产物即为该裁决的全部依据**——不再依赖推理。

## AP. 选项 1 / 2a / 2b 的代价拆解（2026-09-06 第 8 轮；为 `HANDOFF` 裁决矩阵 §二 提供实测依据）

**为什么单独做这一项**：裁决矩阵此前把"加宽正则"写成**一项**，其中**驼峰键**与**散文式**捆在一起。实测后两者代价差一个数量级，故拆开——本节的产物就是拆分的依据。

**① 驼峰赋值位确实是现正则的逃逸面**（**regex 级对照，不是门级实跑**——门级负向对照只在人类选定 2a 后于落地时实跑）

命令（原样复制）：

```powershell
$cur  = 'reasoning_effort`?\s*[:=]\s*"?([a-z]+)"?'
$wide = 'reasoning_?[Ee]ffort`?\s*[:=]\s*"?([A-Za-z]+)"?'
foreach ($p in @($cur,$wide)) { foreach ($s in @('reasoningEffort: medium','reasoning_effort: medium')) { $p + '  =>  ' + ([regex]::Matches($s,$p).Count) + ' hit(s) on [' + $s + ']' } }
```

输出（原始）：

```
reasoning_effort`?\s*[:=]\s*"?([a-z]+)"?  =>  0 hit(s) on [reasoningEffort: medium]
reasoning_?[Ee]ffort`?\s*[:=]\s*"?([A-Za-z]+)"?  =>  1 hit(s) on [reasoningEffort: medium]
reasoning_effort`?\s*[:=]\s*"?([a-z]+)"?  =>  1 hit(s) on [reasoning_effort: medium]
reasoning_?[Ee]ffort`?\s*[:=]\s*"?([A-Za-z]+)"?  =>  1 hit(s) on [reasoning_effort: medium]
```

**② 加宽后在全部域内文件上的增量 = 0，取值集合不变**

命令（原样复制，域 = 与 AC4 门逐字相同的三组路径）：

```powershell
$files  = @(); $files += (Get-ChildItem dsh -Recurse -File -Include *.md,*.txt).FullName
$files += (Resolve-Path 'portable\通用prompt-DSH-v1.txt').Path; $files += (Resolve-Path 'README.md').Path
$cur='reasoning_effort`?\s*[:=]\s*"?([a-z]+)"?'; $wide='reasoning_?[Ee]ffort`?\s*[:=]\s*"?([A-Za-z]+)"?'
$curV=@(); $wideV=@()
foreach ($f in $files) {
  foreach ($x in (Select-String -Path $f -Pattern $cur  -AllMatches).Matches) { $curV  += $x.Groups[1].Value }
  foreach ($x in (Select-String -Path $f -Pattern $wide -AllMatches).Matches) { $wideV += $x.Groups[1].Value }
}
'current  matched values = ' + (($curV|Sort-Object -Unique) -join ', ') + '   (total hits ' + $curV.Count + ')'
'widened  matched values = ' + (($wideV|Sort-Object -Unique) -join ', ') + '   (total hits ' + $wideV.Count + ')'
'widened-minus-current hits = ' + ($wideV.Count - $curV.Count)
```

输出（原始）：

```
current  matched values = high   (total hits 7)
widened  matched values = high   (total hits 7)
widened-minus-current hits = 0
adapter medium hits = 0
```

**③ 由此得到的代价对比（可证的）**

* **驼峰赋值位（选项 2a）**：现正则对 `reasoningEffort: medium` **零命中**（① 第 1 行）→ 逃逸面真实存在；加宽后**域内新增命中 0、取值集合仍只有 `high`**（② 第 3 行）→ 不引入新判据、不改动任何现有行的判定。改动量 = `reasoning_effort` → `reasoning_?[Ee]ffort`、`[a-z]` → `[A-Za-z]`，**1 行**；另需 1 条门级负向对照。
* **散文式（选项 2b）**：与 ① 无关——它要匹配 `` 9A/9B = `high` `` 这类**自由措辞**，代价落在"自由文本匹配"上（本任务六轮来反复出错的那类判据）。
* **因此**把"加宽"拆成 2a（机械、1 行、0 增量）与 2b（脆、扩判据面）**有实测依据**，不是措辞偏好。

**④ 如实标注的限度**：①② 均为 **Author 自跑**，且 ① 是 **regex 级**对照——它证明"逃逸面存在、加宽可堵"，**不**代替门级负向对照（`AC4: FAIL` / `exit=1` 的实跑）。后者在人类选定方向后于落地时实跑并留原始输出；**本节不声称该门已被证明有区分力**。

## AQ. 人类裁决选项 2a：AC4 门加宽到两种拼写的赋值位（含新的门级负向对照）

**裁决**：2026-09-06 人类在 `HANDOFF` 裁决矩阵 §二的三个选项中选 **2a**（把正则加宽到两种拼写的赋值位），并裁**不再开新审查轮**；故本项产物是 Author 侧的真实实跑（`§AP ④` 已事先声明该门级负向对照将在选定后补跑，本节即补齐它）。

**改动**（`git diff --numstat` = `2 1`：1 行正则 + 1 行英文注释）：

```diff
@@ -13,0 +14 @@ $vals = @()
+# Assignment sites are matched for both spellings of the key: reasoning_effort and reasoningEffort.
@@ -15 +16 @@ foreach ($f in $files) {
-  $m = (Select-String -Path $f -Pattern 'reasoning_effort`?\s*[:=]\s*"?([a-z]+)"?' -AllMatches).Matches
+  $m = (Select-String -Path $f -Pattern 'reasoning_?[Ee]ffort`?\s*[:=]\s*"?([A-Za-z]+)"?' -AllMatches).Matches
```

**① 正向（加宽后）**：命令 `pwsh -File tools/ac4-reasoning-effort-check.ps1`，原始输出：

```
adapter            = C:\Users\16097\AppData\Local\npm-cache\_npx\1e7f6d9597241db0\node_modules\@deepseek-ai\dsh-llm-deepseek\lib\index.js
DSH-side values    = high
out-of-domain      = (none)
adapter medium hits= 0
AC4: PASS
```

`exit=0`

**② 负向对照 NC-C（本门第 3 条负向对照）：把驼峰赋值位写进域内文件**——临时向 `dsh/workflow/fanout-toolchain.md` 追加一行（`<!-- NC (temporary, reverted): reasoningEffort: medium -->`）后重跑，原始输出：

```
adapter            = C:\Users\16097\AppData\Local\npm-cache\_npx\1e7f6d9597241db0\node_modules\@deepseek-ai\dsh-llm-deepseek\lib\index.js
DSH-side values    = high, medium
out-of-domain      = medium
adapter medium hits= 0
AC4: FAIL
```

`exit=1` → **加宽后的谓词对驼峰赋值位有区分力**；而 `§AP ①` 已实测**旧谓词对同一行是零命中**。两节合起来才是"逃逸面存在 + 加宽可堵"的完整证据。

**③ 还原与复核**：`git checkout -- dsh/workflow/fanout-toolchain.md` → blob 复核 `cb99d7c2a2eb6862814b4920e1583c2e4f65b308`（与改动前一致）→ 重跑正向仍 `DSH-side values = high / out-of-domain = (none) / AC4: PASS`，`exit=0`。**提交前工作树不含任何负向对照残留**（实测 `git status --porcelain` 只剩本脚本自身的改动与 scratch 条目）。

**④ 仍未覆盖（已在 AC4 声称里写明，非暗账）**：**散文式取值陈述**（如 `` 9A/9B = `high` ``）不在门的域内——人类在三个选项中**未选 2b**，故该面按"AC4 只声明赋值位（两种拼写）"如实收窄，**不再声称覆盖**。

## AR. PB-2 偿还：AC4 门的 checkout 根改为从脚本自身位置推导（含"异 checkout 假绿"对照）

**裁决**：2026-09-06 人类裁决"修路径推导 + 把声称改准"（`HANDOFF` §五）。本笔债的 Payback trigger 是"下次改动该脚本之前"，而选项 **2a** 刚刚改动过它 → 按 Payback-on-Touch 当场偿还。

**改动**（`git diff --numstat` = `1 1`）：

```diff
@@ -2 +2 @@ $ErrorActionPreference = 'Stop'
-$repo = 'C:\Users\16097\Desktop\workflow'
+$repo = Split-Path -Parent $PSScriptRoot  # <checkout>/tools -> <checkout>
```

**① 修复前的真实失效形态是"假绿"，不是"报错终止"（本对照推翻了此前账目里的机制描述）**

做法：把 `HEAD` 版门脚本（`$repo` 硬绑本机路径）放进一个与仓库无关的副本目录（`%TEMP%\ac4-prefix`，内含 `tools/`、`dsh/`、`portable/`、`README.md`），**只给副本的 `README.md` 注入一行 `reasoning_effort: medium`**，然后在 `cwd = C:\` 下运行**副本里的**脚本。原始输出：

```
adapter            = C:\Users\16097\AppData\Local\npm-cache\_npx\1e7f6d9597241db0\node_modules\@deepseek-ai\dsh-llm-deepseek\lib\index.js
DSH-side values    = high
out-of-domain      = (none)
adapter medium hits= 0
AC4: PASS
```

`exit=0`——**副本 README 里明明有 `medium`，门却报 PASS**：它读的是脚本里写死的本机仓库，不是调用者的 checkout。**故此前账目写的"换 checkout 会在 `Set-Location` 处直接终止、拿不到 verdict"不准确**：同一台机器上换 checkout **不会**终止，而是**静默读错树、给出假绿**（比终止更危险）；只有换到不存在该硬编码路径的机器才会终止。账目已按实测订正。

**② 修复后的三组实跑**

`A. 异 cwd（cwd = C:\，调用本仓脚本）`：

```
DSH-side values    = high
out-of-domain      = (none)
adapter medium hits= 0
AC4: PASS
```

`exit=0`。**本项不具区分力**——本机硬编码路径恰好就是本仓，故它与"读对树"结果相同；区分力来自 B/C。

`B / C. 异 checkout 副本（同一副本两次运行，差别只有副本 README 里那一行）`：

```
B. 副本未改动        → DSH-side values = high         / out-of-domain = (none)   → AC4: PASS
C. 只改副本 README   → DSH-side values = high, medium / out-of-domain = medium   → AC4: FAIL
```

原始退出码：`B` = `exit=0`、`C` = `exit=1`。**C 的 FAIL 证明脚本读的是调用者所在的 checkout**——若它仍读原仓，C 必然与 B 一样假绿。副本用后即删（实测 `Test-Path = False`）。

`D. 本仓正向复跑`：`DSH-side values = high / out-of-domain = (none) / adapter medium hits = 0 → AC4: PASS`，`exit=0`。

**③ 改准后的声称**：AC4 判定命令**在任意 checkout 可执行**（`$repo` 由脚本位置推导）。仍余一条环境依赖：适配器路径取自 `%LOCALAPPDATA%\npm-cache\_npx\*`，**需本机存在 npx 缓存的 dsh 适配器**；找不到时门报错退出（**非静默通过**）。此限度已写入 `TASK_BRIEF` AC4。

**④ 与本文件其余各节的关系**：`§AQ` 是同一脚本在同一天的另一次改动（2a 加宽）。`§AQ` 证明**谓词有区分力**，本节证明**这道门在哪棵树上执行是确定的**——两者互补，缺任一条这道 `[M]` 门的声称都不完整。

## AS. 交接前复核（`ac16fad` 之上的工作树，2026-09-06）：两道 `[M]` 门 + 工作树 + `~/.dsh` 同步面

**为什么这一节存在**：人类裁决「合并 / push 由人类执行」之后，Author 在交接前把两道门、工作树与部署副本同步面在同一棵树上重跑一遍，并记下一处**新发现的自失效数字**（④）。**本节只把一处自失效的既有陈述改成可复核形态，未新增判据 / 登记表 / AC。**

**① AC4-门（正向，真实退出码）**——与本节同一次运行中执行（即 ④ 的改动已在工作树里）：

```
adapter            = C:\Users\16097\AppData\Local\npm-cache\_npx\1e7f6d9597241db0\node_modules\@deepseek-ai\dsh-llm-deepseek\lib\index.js
DSH-side values    = high
out-of-domain      = (none)
adapter medium hits= 0
AC4: PASS
exit=0
```

**② AC6-门（判定块逐字取自 `TASK_BRIEF.md` AC6，`base = bf06c65d0831ebeb2b0982f35ae2d7f4c65c2c17`）**

```
reg=31 scope=31 missing=0 stale=0
AC6: register == scope
exit=0
```

**③ 工作树与部署副本（`~/.dsh`）**

```
$ git status --porcelain=v1 -uall
 M README.md
 M docs/ai/HANDOFF.md          # 即本次订正自身；无未跟踪文件（第 6 轮 .tmp-r6.ps1 的教训项）
$ repo->~/.dsh pairs=23 same=23 differing=0 missing=0
$ ~/.dsh/workflow 下多出的文件 → none
```

即 `~/.dsh/{AGENTS.md, workflow/**, skills/{dual-agent-workflow,independent-review}/**}` 与仓内 `dsh/**` **逐字节一致（23/23，无多余文件）**——README 所记的"人工等价部署"在本 tip 上**未漂移**。

**④ 新发现：写死的提交数已自失效（与第 7 轮 9B 的 PB-1 同类）**

`README.md:54` 与 `HANDOFF.md:7/96/219` 原文写 `origin/main..HEAD = 37`。实测：

```
$ git rev-list --count origin/main..HEAD
38
$ git rev-list --count main..HEAD
0
```

差 1 的成因是**自指**：写下「37」的那笔提交（`ac16fad`）自身就是第 38 笔——**任何"记录当前提交数"的改动都会使自己记录的数字过期**。这与 PB-1 的教训（"写死数字必然随加账过期"）同源，只是失效机制更机械（不是加账后才错，而是**写下即错**）。**处置**：4 处一律改为**现查命令** `git rev-list --count origin/main..HEAD`，不再写死笔数。

## AT. push 完成后的核验（2026-09-06）：本机 ref 证据 + 两道 `[M]` 门复跑 + 一处空指订正

**为什么这一节存在**：人类执行 push 之后，账本里 3 处"剩余动作 = 人类 push"成了**状态过期**陈述（与第 7 轮 9B 对 `HANDOFF` 提的"状态过期"同类）。本节是该笔终账的证据面，同时记录一处**空指订正**（④）。

**① 本机 ref 证据（**不是**网络回读）**

```
$ git rev-parse main / HEAD / origin/main
main        = 9a10db740c793ad9bc7f1a5ea5974315ea79fb87
HEAD        = 9a10db740c793ad9bc7f1a5ea5974315ea79fb87
origin/main = 9a10db740c793ad9bc7f1a5ea5974315ea79fb87
$ git rev-list --count origin/main..HEAD   → 0
$ git rev-list --count HEAD..origin/main   → 0
$ git branch -vv
* main 9a10db7 [origin/main] docs: stop hardcoding the pre-push commit count
$ git reflog show origin/main | Select-Object -First 2
9a10db7 refs/remotes/origin/main@{0}: update by push
bf06c65 refs/remotes/origin/main@{1}: update by push
```

（快照：本次核验时被推送的 tip 即 `origin/main@{0}`；**再次 push 后本条会下移一位**，属正常。）

**如实标注的限度**：本仓契约禁止 agent 做任何远程操作（含 `fetch` / `ls-remote`），**故 agent 未独立复核远端**。"远端确实收到 `9a10db7`"由人类陈述 + 本机 `update by push` 共同担保；这是 **ref 级**证据，不是网络回读。

**② 两道 `[M]` 门在同一工作树上复跑（真实退出码）**

AC4-门：

```
adapter            = C:\Users\16097\AppData\Local\npm-cache\_npx\1e7f6d9597241db0\node_modules\@deepseek-ai\dsh-llm-deepseek\lib\index.js
DSH-side values    = high
out-of-domain      = (none)
adapter medium hits= 0
AC4: PASS
exit=0
```

AC6-门（判定块逐字取自 `TASK_BRIEF.md` AC6，`base = bf06c65d0831ebeb2b0982f35ae2d7f4c65c2c17`）：

```
reg=31 scope=31 missing=0 stale=0
AC6: register == scope
exit=0
```

**③ 工作树**：`git status --porcelain=v1 -uall` = ` M README.md` / ` M docs/ai/HANDOFF.md`（即本次终账自身；**无未跟踪文件**）。

**④ 订正一处空指："（+ CI）"**

```
$ Test-Path .github                                                        → False
$ 搜索 .github / .gitlab-ci.yml / azure-pipelines.yml / appveyor.yml / .travis.yml → 无命中
```

本仓**没有任何 CI 配置**，故原文"push 到 `origin/main`（+ CI）"中的"（+ CI）"是**空指**——push 不会触发流水线。`README.md` 与 `HANDOFF` §五 的对应处已同时订正。**这与 PB-1 / PB-2 同类：不是行为缺陷，而是"说与做不一致"的账目缺陷。**
