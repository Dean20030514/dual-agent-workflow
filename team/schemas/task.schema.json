{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "result_schema_sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "schema_version": {
      "const": 1
    },
    "run_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]{0,79}$"
    },
    "task_id": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]{0,79}$"
    },
    "role": {
      "type": "object",
      "properties": {
        "definition": {"type": "object"},
        "id": {
          "type": "string",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]{0,79}$"
        }
      },
      "required": [
        "id"
      ],
      "additionalProperties": false
    },
    "objective": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    },
    "dependencies": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "permissions": {
      "type": "object",
      "properties": {
        "shell": {
          "type": "boolean"
        },
        "network": {
          "type": "boolean"
        },
        "secrets": {
          "type": "boolean"
        },
        "production": {
          "type": "boolean"
        }
      },
      "required": [
        "shell",
        "network",
        "secrets",
        "production"
      ],
      "additionalProperties": false
    },
    "write_scope": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "acceptance": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    },
    "subagents": {
      "type": "object",
      "properties": {
        "allowed": {
          "type": "boolean"
        },
        "max_depth": {
          "type": "integer",
          "minimum": 0,
          "maximum": 2
        }
      },
      "required": [
        "allowed",
        "max_depth"
      ],
      "additionalProperties": false
    },
    "base_sha": {
      "type": "string",
      "minLength": 1
    },
    "reuse": {
      "type": "object",
      "description": "Task-level reuse declaration copied from the frozen plan. Historical packets omit it; new dispatches always include it.",
      "additionalProperties": true
    },
    "reuse_context": {
      "type": "object",
      "description": "Derived bounded reuse context (protocol identity, plan hash, decision fields, task declaration and only the referenced candidates). Present for every new dispatch; the Result must then carry a matching reuse declaration.",
      "additionalProperties": true
    },
    "result_schema": {
      "type": "string",
      "minLength": 1
    },
    "issue_acceptance_map": {
      "type": "array",
      "description": "Optional author-declared mapping from objective entries to acceptance entries. The runner never invents issue text.",
      "items": {
        "type": "object",
        "properties": {
          "objective_index": {
            "type": "integer",
            "minimum": 0
          },
          "acceptance_indexes": {
            "type": "array",
            "items": {
              "type": "integer",
              "minimum": 0
            },
            "minItems": 1
          }
        },
        "required": [
          "objective_index",
          "acceptance_indexes"
        ],
        "additionalProperties": false
      }
    },
    "verification": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]{0,79}$"
          },
          "executable": {
            "type": "string",
            "minLength": 1
          },
          "args": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "timeout_seconds": {
            "type": "integer",
            "minimum": 1
          },
          "environment_fingerprint": {
            "type": "string",
            "minLength": 1,
            "description": "Caller-declared environment identity (for example an isolated database fingerprint). Deterministic reuse requires it; declare 'none' only for hermetic commands."
          },
          "input_artifacts": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            },
            "description": "Workspace-relative input files whose hashes are bound into a reuse key."
          }
        },
        "required": [
          "id",
          "executable",
          "args",
          "timeout_seconds"
        ],
        "additionalProperties": false
      },
      "minItems": 1
    }
  },
  "required": [
    "schema_version",
    "run_id",
    "task_id",
    "role",
    "objective",
    "dependencies",
    "permissions",
    "write_scope",
    "acceptance",
    "subagents",
    "base_sha",
    "result_schema",
    "verification"
  ],
  "additionalProperties": false
}
